# Legacy archives in public/ — not deployed to Vercel edge (large static archives)
# Do NOT use bare `data/` — that also excludes src/data/categories.ts and breaks the build.
public/data/
public/files/
