# Legacy archives in public/ — mirror FTP deploy exclude (deploy-ftp.yml: **/data/**, **/files/**)
# Do NOT use bare `data/` — that also excludes src/data/categories.ts and breaks the build.
public/data/
public/files/
