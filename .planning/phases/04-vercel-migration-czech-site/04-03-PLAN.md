---
phase: 04-vercel-migration-czech-site
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - scripts/verify-phase4-vercel.sh
  - scripts/verify-phase3-parity.sh
autonomous: true
requirements:
  - VD-03
must_haves:
  truths:
    - "Operator can run verify-phase4-vercel.sh against a Vercel preview URL and get pass/fail for redirects"
    - "All 26 redirect-samples.txt cases are checked against VERCEL_PREVIEW_URL not just production"
    - "Font CORS header verified on preview for a .woff2 asset path"
    - "Trailing-slash policy verified: /path/ returns 308/301 to /path on preview"
  artifacts:
    - path: scripts/verify-phase4-vercel.sh
      provides: "Vercel preview parity gate"
      min_lines: 80
  key_links:
    - from: scripts/verify-phase4-vercel.sh
      to: scripts/redirect-samples.txt
      via: "same pipe-delimited matrix as verify-phase3-parity.sh"
      pattern: "redirect-samples\\.txt"
    - from: scripts/verify-phase4-vercel.sh
      to: apps/vzhurudolu/vercel.json
      via: "VERCEL_PREVIEW_URL env targets deployed preview with ported redirects"
      pattern: "VERCEL_PREVIEW_URL"
---

<objective>
Create automated Vercel preview verification gate extending Phase 3 redirect sample matrix for preview URL testing.

Purpose: ROADMAP success criterion #1 requires preview matches production URL behavior. Phase 3 script only curls production; Phase 4 needs the same matrix against Vercel preview (D-13 scope from Phase 3).
Output: `scripts/verify-phase4-vercel.sh` with VERCEL_PREVIEW_URL support.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-vercel-migration-czech-site/04-CONTEXT.md
@scripts/verify-phase3-parity.sh
@scripts/redirect-samples.txt
@apps/vzhurudolu/vercel.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verify-phase4-vercel.sh scaffold</name>
  <files>scripts/verify-phase4-vercel.sh</files>
  <read_first>
    - scripts/verify-phase3-parity.sh (normalize_location_suffix, redirect sample loop lines 274-309)
    - scripts/redirect-samples.txt
    - apps/vzhurudolu/vercel.json (trailingSlash, headers)
  </read_first>
  <acceptance_criteria>
    - scripts/verify-phase4-vercel.sh exists, executable, set -euo pipefail
    - Requires VERCEL_PREVIEW_URL env var (https://*.vercel.app or custom preview host); exits 1 with clear message if unset
    - Strips trailing slash from VERCEL_PREVIEW_URL base
    - Reuses redirect-samples.txt format: source_path|expected_location_suffix
    - Accepts 301 or 308 for redirect status (matches Phase 3 production check)
    - normalize_location_suffix handles preview host in Location header (strip VERCEL_PREVIEW_URL origin)
    - macOS bash 3.2 compatible (no mapfile)
  </acceptance_criteria>
  <action>
    Create scripts/verify-phase4-vercel.sh modeled on verify-phase3-parity.sh redirect loop. Read VERCEL_PREVIEW_URL from environment (required). For each redirect-samples.txt row, curl -sfI VERCEL_PREVIEW_URL+source_path, assert HTTP 301 or 308, parse Location header, compare path suffix to expected_suffix using same normalize logic as Phase 3 but strip preview origin instead of vzhurudolu.cz. Print pass/fail counts; exit 1 if any fail. Support optional --redirects-only flag skipping content checks (for Plan 04-04 before full deploy). Do not require local build if only testing live preview.
  </action>
  <verify>
    <automated>test -x scripts/verify-phase4-vercel.sh && bash -n scripts/verify-phase4-vercel.sh && VERCEL_PREVIEW_URL= bash scripts/verify-phase4-vercel.sh 2>&1 | grep -qi 'VERCEL_PREVIEW_URL'</automated>
  </verify>
  <done>verify-phase4-vercel.sh validates redirect matrix against preview URL when env set</done>
</task>

<task type="auto">
  <name>Task 2: Add header and trailing-slash checks</name>
  <files>scripts/verify-phase4-vercel.sh</files>
  <read_first>
    - scripts/verify-phase4-vercel.sh (Task 1 output)
    - apps/vzhurudolu/vercel.json (headers, trailingSlash)
    - apps/vzhurudolu/public/.htaccess (trailing slash rule lines 188-191)
    - apps/vzhurudolu/public/assets (find a .woff2 path for CORS probe)
  </read_first>
  <acceptance_criteria>
    - Script includes check_trailing_slash: curl -sI VERCEL_PREVIEW_URL/prirucka/css-flexbox/ (or another known dist path with trailing slash) returns 301 or 308 with Location ending without trailing slash
    - Script includes check_font_cors: curl -sI on a known .woff2 URL under /assets/ returns Access-Control-Allow-Origin: *
    - Script includes check_homepage_200: curl -sf VERCEL_PREVIEW_URL/ returns HTTP 200
    - Full run (without --redirects-only) executes redirect matrix + three checks above
    - verify-phase3-parity.sh unchanged in behavior (only add comment cross-reference at top if helpful — no functional regression)
  </acceptance_criteria>
  <action>
    Extend verify-phase4-vercel.sh with three additional checks after redirect matrix: (1) trailing slash — request a known content path with trailing slash, expect redirect to slashless URL; (2) font CORS — discover first .woff2 under apps/vzhurudolu/public/assets via find, request that path on preview, grep Access-Control-Allow-Origin * in response headers; (3) homepage 200. Gate fails if any check fails. Document usage in script header comment: VERCEL_PREVIEW_URL=https://xxx.vercel.app bash scripts/verify-phase4-vercel.sh
  </action>
  <verify>
    <automated>bash -n scripts/verify-phase4-vercel.sh && grep -q 'trailing' scripts/verify-phase4-vercel.sh && grep -q 'Access-Control' scripts/verify-phase4-vercel.sh && grep -q 'redirect-samples' scripts/verify-phase4-vercel.sh</automated>
  </verify>
  <done>Preview gate covers redirects, trailing slash, font CORS, and homepage availability</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/local → Vercel preview URL | curl requests to external preview host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Spoofing | VERCEL_PREVIEW_URL injection | mitigate | Script only accepts env var set by operator/CI secret; no user input concatenation |
| T-04-08 | Denial of Service | curl loop to preview | accept | 26 samples + 3 checks; max-time 15s per curl like Phase 3 |
| T-04-SC | Tampering | npm installs | accept | Uses curl/bash only |
</threat_model>

<verification>
bash -n scripts/verify-phase4-vercel.sh
grep -c 'redirect-samples' scripts/verify-phase4-vercel.sh | awk '{exit ($1<1)?1:0}'
</verification>

<success_criteria>
- verify-phase4-vercel.sh passes bash syntax check
- Redirect, trailing-slash, CORS, and homepage checks implemented
</success_criteria>

<artifacts_this_phase_produces>
- scripts/verify-phase4-vercel.sh
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-vercel-migration-czech-site/04-03-SUMMARY.md` when done
</output>
