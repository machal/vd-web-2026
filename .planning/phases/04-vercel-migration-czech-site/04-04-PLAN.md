---
phase: 04-vercel-migration-czech-site
plan: 04
type: execute
wave: 3
depends_on:
  - "04-02"
  - "04-03"
files_modified:
  - .github/workflows/deploy-ftp.yml
  - .github/workflows/pr-build.yml
  - .planning/phases/04-vercel-migration-czech-site/04-VERIFICATION.md
autonomous: false
requirements:
  - VD-03
user_setup:
  - service: vercel
    why: "Create Vercel project linked to GitHub repo for Czech app"
    dashboard_config:
      - task: "Import Git repository"
        location: "Vercel Dashboard → Add New Project"
      - task: "Set Root Directory to apps/vzhurudolu"
        location: "Project Settings → General → Root Directory"
      - task: "Enable Include source files outside Root Directory (monorepo)"
        location: "Project Settings → General"
      - task: "Add domain vzhurudolu.cz as preview only (DNS cutover is Phase 9)"
        location: "Project Settings → Domains (optional for preview testing)"
must_haves:
  truths:
    - "Vercel preview deployment succeeds for apps/vzhurudolu"
    - "verify-phase4-vercel.sh passes against live preview URL with 26/26 redirects"
    - "FTP deploy workflow remains active as rollback path (unchanged trigger and paths)"
    - "Phase 4 VERIFICATION.md documents preview URL and rollback procedure"
  artifacts:
    - path: .planning/phases/04-vercel-migration-czech-site/04-VERIFICATION.md
      provides: "Phase gate evidence and rollback notes"
      contains: "status:"
  key_links:
    - from: .github/workflows/deploy-ftp.yml
      to: apps/vzhurudolu/dist
      via: "push main still deploys FTP — rollback path preserved"
      pattern: "FTP-Deploy-Action"
    - from: scripts/verify-phase4-vercel.sh
      to: Vercel preview deployment
      via: "VERCEL_PREVIEW_URL from dashboard deployment URL"
      pattern: "verify-phase4-vercel"
---

<objective>
Wire Phase 4 gate: Vercel project connected, preview verified, FTP rollback preserved, phase VERIFICATION.md complete.

Purpose: VD-03 delivery requires working Vercel preview with parity proof while FTP stays rollback until Phase 9 (CONTEXT deferred DNS cutover).
Output: Passing preview verification, 04-VERIFICATION.md, confirmed unchanged FTP workflow.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-vercel-migration-czech-site/04-CONTEXT.md
@.planning/ROADMAP.md
@.github/workflows/deploy-ftp.yml
@.github/workflows/pr-build.yml
@scripts/verify-phase4-vercel.sh
@scripts/verify-phase3-parity.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm FTP rollback preserved and document Vercel setup</name>
  <files>.planning/phases/04-vercel-migration-czech-site/04-VERIFICATION.md, .github/workflows/deploy-ftp.yml</files>
  <read_first>
    - .github/workflows/deploy-ftp.yml (full file — must remain active)
    - .planning/phases/04-vercel-migration-czech-site/04-CONTEXT.md (FTP rollback, Phase 9 DNS deferred)
    - .planning/ROADMAP.md Phase 4 success criteria
    - apps/vzhurudolu/vercel.json
  </read_first>
  <acceptance_criteria>
    - .github/workflows/deploy-ftp.yml still triggers on push to main/master and deploys apps/vzhurudolu/dist/
    - deploy-ftp.yml exclude list still contains data/** and files/**
    - 04-VERIFICATION.md created with frontmatter status: pending (updated to passed after Task 2 checkpoint)
    - 04-VERIFICATION.md documents: Vercel Root Directory apps/vzhurudolu, monorepo setting, preview URL placeholder, rollback = keep using FTP deploy (revert DNS in Phase 9)
    - pr-build.yml NOT modified to require VERCEL_PREVIEW_URL (preview gate is manual/local until Vercel project exists — avoids CI failure without secrets)
  </acceptance_criteria>
  <action>
    Verify deploy-ftp.yml is unchanged in intent (do not remove or disable FTP workflow). Create .planning/phases/04-vercel-migration-czech-site/04-VERIFICATION.md with YAML frontmatter (status: pending), checklist mirroring ROADMAP Phase 4 success criteria, Vercel dashboard setup steps from user_setup frontmatter, and rollback section stating FTP remains production path until Phase 9 DNS cutover. Do not delete .htaccess from public/ — it remains for FTP/Apache production until Phase 9.
  </action>
  <verify>
    <automated>test -f .github/workflows/deploy-ftp.yml && grep -q 'FTP-Deploy-Action' .github/workflows/deploy-ftp.yml && test -f .planning/phases/04-vercel-migration-czech-site/04-VERIFICATION.md && grep -q 'rollback' .planning/phases/04-vercel-migration-czech-site/04-VERIFICATION.md</automated>
  </verify>
  <done>VERIFICATION.md documents setup and rollback; FTP workflow confirmed active</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Connect Vercel project and run preview gate</name>
  <files>.planning/phases/04-vercel-migration-czech-site/04-VERIFICATION.md</files>
  <read_first>
    - apps/vzhurudolu/vercel.json
    - scripts/verify-phase4-vercel.sh
    - .planning/phases/04-vercel-migration-czech-site/04-VERIFICATION.md
  </read_first>
  <acceptance_criteria>
    - Vercel project exists with Root Directory apps/vzhurudolu and monorepo setting enabled
    - Latest deployment build log shows successful turbo build and dist/_astro output
    - VERCEL_PREVIEW_URL=https://YOUR-PREVIEW.vercel.app bash scripts/verify-phase4-vercel.sh exits 0
    - Redirect sample summary shows 26 pass, 0 fail
  </acceptance_criteria>
  <action>
    Human-only: Vercel Dashboard → Add New Project → import GitHub repo. Set Root Directory apps/vzhurudolu. Enable Include source files outside Root Directory. Trigger deploy. Copy preview URL. Run VERCEL_PREVIEW_URL=https://YOUR-PREVIEW.vercel.app bash scripts/verify-phase4-vercel.sh locally. Resume with preview URL and "approved" when script passes, or describe failures.
  </action>
  <verify>
    <automated>MISSING — Wave 3 checkpoint; human runs verify-phase4-vercel.sh against live preview URL</automated>
  </verify>
  <done>Vercel preview deployed; verify-phase4-vercel.sh exit 0 with 26/26 redirects</done>
  <what-built>Vercel-ready config (vercel.json, .vercelignore), redirect port, verify-phase4-vercel.sh</what-built>
  <how-to-verify>
    1. Vercel Dashboard → Add New Project → import this GitHub repo
    2. Set Root Directory: apps/vzhurudolu
    3. Enable "Include source files outside of Root Directory" (monorepo)
    4. Deploy — confirm build succeeds and dist/_astro exists in deployment logs
    5. Copy preview URL (e.g. https://vd-web-2026-xxx.vercel.app)
    6. Run locally:
       VERCEL_PREVIEW_URL=https://YOUR-PREVIEW.vercel.app bash scripts/verify-phase4-vercel.sh
    7. Expect: 26/26 redirect samples pass, trailing-slash check pass, font CORS pass, homepage 200
    8. Optional spot-check: open /prirucka/css-flexbox and /blog/261-rok-2025 on preview — content renders
  </how-to-verify>
  <resume-signal>Type preview URL and "approved" after verify-phase4-vercel.sh passes, or describe failures</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize VERIFICATION.md after preview pass</name>
  <files>.planning/phases/04-vercel-migration-czech-site/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/04-vercel-migration-czech-site/04-VERIFICATION.md
    - scripts/verify-phase4-vercel.sh output from checkpoint
  </read_first>
  <acceptance_criteria>
    - 04-VERIFICATION.md frontmatter status: passed
    - Documents actual VERCEL_PREVIEW_URL used (redact if sensitive)
    - Records verify-phase4-vercel.sh exit 0 and redirect sample count 26/26
    - Confirms FTP workflow still primary production deploy path
    - Lists any WARN items (e.g. www redirect handled by Vercel domains not vercel.json)
  </acceptance_criteria>
  <action>
    After human checkpoint approval, update 04-VERIFICATION.md: set status passed, paste verify script summary output, record preview URL, mark each ROADMAP success criterion checked. Note production vzhurudolu.cz still served via FTP until Phase 9.
  </action>
  <verify>
    <automated>grep -q 'status: passed' .planning/phases/04-vercel-migration-czech-site/04-VERIFICATION.md && grep -q '26' .planning/phases/04-vercel-migration-czech-site/04-VERIFICATION.md</automated>
  </verify>
  <done>Phase 4 VERIFICATION.md status passed with preview gate evidence</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Dashboard → GitHub | OAuth repo access for deployments |
| Preview URL → public internet | Unlisted preview may leak content — acceptable for static marketing site |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Elevation | Vercel project permissions | transfer | User controls dashboard access; no secrets in repo |
| T-04-10 | Denial of Service | Preview deploy failure | mitigate | verify buildCommand locally before dashboard; turbo filter gate |
| T-04-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
test -f .planning/phases/04-vercel-migration-czech-site/04-VERIFICATION.md
grep -q 'FTP' .planning/phases/04-vercel-migration-czech-site/04-VERIFICATION.md
</verification>

<success_criteria>
- Vercel preview deployed and verify-phase4-vercel.sh passes
- FTP deploy workflow preserved
- 04-VERIFICATION.md status passed
</success_criteria>

<artifacts_this_phase_produces>
- .planning/phases/04-vercel-migration-czech-site/04-VERIFICATION.md
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-vercel-migration-czech-site/04-04-SUMMARY.md` when done
</output>
