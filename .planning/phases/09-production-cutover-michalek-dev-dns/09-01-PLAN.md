---
phase: 09-production-cutover-michalek-dev-dns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ROLLBACK.md
  - .planning/phases/09-production-cutover-michalek-dev-dns/09-VERIFICATION.md
autonomous: true
requirements:
  - DEPLOY-02
  - DEPLOY-04
must_haves:
  truths:
    - "Rollback runbook documents DNS revert and FTP re-enable path"
    - "09-VERIFICATION.md lists staged DNS and Vercel domain attach for both projects"
    - "09-VERIFICATION.md status is human_needed until DNS cutover complete"
  artifacts:
    - path: docs/ROLLBACK.md
      provides: "Production rollback runbook (DEPLOY-04)"
      contains: "revert DNS"
    - path: .planning/phases/09-production-cutover-michalek-dev-dns/09-VERIFICATION.md
      provides: "Phase 9 human gate checklist (DEPLOY-02)"
      contains: "status: human_needed"
---

## Phase Goal

Document staged DNS cutover, Vercel production domain attach for both monorepo projects, and rollback procedures before disabling FTP.

<objective>
Create docs/ROLLBACK.md and 09-VERIFICATION.md with human-needed DNS/Vercel steps per CONTEXT (DEPLOY-02, DEPLOY-04).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Rollback runbook and verification checklist</name>
  <files>docs/ROLLBACK.md, .planning/phases/09-production-cutover-michalek-dev-dns/09-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/09-production-cutover-michalek-dev-dns/09-CONTEXT.md
    - .planning/phases/04-vercel-migration-czech-site/04-VERIFICATION.md
    - .planning/phases/05-english-app-scaffold/05-VERIFICATION.md
    - .github/workflows/deploy-ftp.yml
  </read_first>
  <acceptance_criteria>
    - docs/ROLLBACK.md exists with sections: When to rollback, DNS revert steps, Vercel redeploy previous build, FTP re-enable from archived workflow
    - docs/ROLLBACK.md states GitHub FTP secrets are NOT deleted until human confirms stable production
    - 09-VERIFICATION.md frontmatter status: human_needed
    - 09-VERIFICATION.md documents TTL reduction 24-48h before switch (DEPLOY-02)
    - 09-VERIFICATION.md lists Vercel domain attach for vzhuru-dolu-2026 (vzhurudolu.cz + www) and michalek-dev (michalek.dev)
    - 09-VERIFICATION.md references scripts/verify-phase9-cutover.sh for post-cutover smoke tests
    - 09-VERIFICATION.md includes placeholder for DNS record values from Vercel dashboard (A/CNAME)
  </acceptance_criteria>
  <action>
    Create docs/ROLLBACK.md at repo root with rollback within 48h window: revert DNS at registrar to prior FTP/Apache records, optionally redeploy prior Vercel deployment from dashboard, re-enable .github/workflows/deploy-ftp.yml by restoring push trigger per archived file comment. Create 09-VERIFICATION.md mirroring 04/05 verification format: YAML frontmatter status human_needed, staged cutover checklist (TTL, simultaneous both domains), Vercel project table (CS vzhuru-dolu-2026 apps/vzhurudolu, EN michalek-dev apps/michalek-dev), domain attach steps, post-cutover verify script command, ROADMAP success criteria unchecked until human completes DNS.
  </action>
  <verify>
    <automated>test -f docs/ROLLBACK.md && grep -q 'revert DNS' docs/ROLLBACK.md && test -f .planning/phases/09-production-cutover-michalek-dev-dns/09-VERIFICATION.md && grep -q 'status: human_needed' .planning/phases/09-production-cutover-michalek-dev-dns/09-VERIFICATION.md && grep -q 'verify-phase9-cutover' .planning/phases/09-production-cutover-michalek-dev-dns/09-VERIFICATION.md</automated>
  </verify>
  <done>Rollback runbook and human-needed verification checklist committed</done>
</task>

</tasks>

## Artifacts this phase produces

- `docs/ROLLBACK.md` — production rollback runbook
- `.planning/phases/09-production-cutover-michalek-dev-dns/09-VERIFICATION.md` — DNS/Vercel human gate
