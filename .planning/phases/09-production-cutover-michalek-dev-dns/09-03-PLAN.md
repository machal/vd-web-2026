---
phase: 09-production-cutover-michalek-dev-dns
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - .github/workflows/deploy-ftp.yml
  - .github/workflows/archived/deploy-ftp.yml
autonomous: true
requirements:
  - DEPLOY-03
must_haves:
  truths:
    - "FTP deploy no longer runs automatically on push to main/master"
    - "Archived workflow file preserved with rollback instructions pointing to docs/ROLLBACK.md"
    - "Active deploy-ftp.yml disabled with comment explaining archive location"
  artifacts:
    - path: .github/workflows/archived/deploy-ftp.yml
      provides: "Preserved FTP workflow for rollback (DEPLOY-03)"
      contains: "FTP-Deploy-Action"
    - path: .github/workflows/deploy-ftp.yml
      provides: "Disabled FTP trigger stub"
      contains: "ROLLBACK.md"
---

## Phase Goal

Retire automatic FTP production deploy while preserving one-command rollback path.

<objective>
Archive deploy-ftp.yml and replace active workflow with disabled stub referencing docs/ROLLBACK.md (DEPLOY-03).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Archive FTP workflow and disable auto-deploy</name>
  <files>.github/workflows/deploy-ftp.yml, .github/workflows/archived/deploy-ftp.yml</files>
  <read_first>
    - .github/workflows/deploy-ftp.yml
    - docs/ROLLBACK.md
    - .planning/phases/09-production-cutover-michalek-dev-dns/09-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - .github/workflows/archived/deploy-ftp.yml contains full original FTP workflow (push to main/master trigger, turbo build, FTP-Deploy-Action)
    - .github/workflows/deploy-ftp.yml no longer triggers on push (only workflow_dispatch or fully commented on block)
    - deploy-ftp.yml top comment references docs/ROLLBACK.md and archived/deploy-ftp.yml restore path
    - docs/ROLLBACK.md FTP section updated to mention archived path if not already
  </acceptance_criteria>
  <action>
    Copy current deploy-ftp.yml to .github/workflows/archived/deploy-ftp.yml with header comment ARCHIVED Phase 9 — restore per docs/ROLLBACK.md. Replace deploy-ftp.yml with stub: comment block explaining retirement, on workflow_dispatch only (manual rollback deploy), same job steps as archive OR pointer comment that operator copies archived file back. Prefer workflow_dispatch retaining full deploy steps so rollback is one enable-push away without file copy.
  </action>
  <verify>
    <automated>test -f .github/workflows/archived/deploy-ftp.yml && grep -q 'FTP-Deploy-Action' .github/workflows/archived/deploy-ftp.yml && ! grep -q 'branches: \[master, main\]' .github/workflows/deploy-ftp.yml || grep -q 'workflow_dispatch' .github/workflows/deploy-ftp.yml</automated>
  </verify>
  <done>FTP workflow archived and auto-deploy disabled</done>
</task>

</tasks>

## Artifacts this phase produces

- `.github/workflows/archived/deploy-ftp.yml` — preserved FTP deploy workflow
- `.github/workflows/deploy-ftp.yml` — disabled stub with rollback pointer
