---
phase: 09-production-cutover-michalek-dev-dns
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase9-cutover.sh
autonomous: true
requirements:
  - DEPLOY-02
must_haves:
  truths:
    - "Production smoke script curls both domains with configurable base URLs"
    - "Script checks CS homepage, redirect samples, RSS, sitemap"
    - "Script checks EN homepage, sample routes, RSS, sitemap"
  artifacts:
    - path: scripts/verify-phase9-cutover.sh
      provides: "Phase 9 production URL smoke gate"
      min_lines: 80
---

## Phase Goal

Automated production smoke tests for post-DNS cutover verification on both domains.

<objective>
Create scripts/verify-phase9-cutover.sh following verify-phase4-vercel.sh patterns with env vars for production URLs (DEPLOY-02).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Production cutover smoke test script</name>
  <files>scripts/verify-phase9-cutover.sh</files>
  <read_first>
    - scripts/verify-phase4-vercel.sh
    - scripts/verify-phase8-launch-polish.sh
    - scripts/redirect-samples.txt
    - packages/shared/content-pairing/pairs.ts
  </read_first>
  <acceptance_criteria>
    - Script accepts VZHURUDOLU_URL (default https://www.vzhurudolu.cz) and MICHALEK_DEV_URL (default https://michalek.dev)
    - Script exits non-zero on any failed check with clear FAIL message
    - CS checks: GET / returns 200, at least 3 redirect samples from redirect-samples.txt return 301/308 with expected Location suffix, /rss returns 200 with rss/xml content-type or xml body, /sitemap.xml returns 200
    - EN checks: GET / returns 200, /martin returns 200, /guide/webp and /blog/2025-year-in-review return 200, /rss returns 200, /sitemap.xml returns 200
    - Script is executable (chmod +x) and documents usage in header comment
  </acceptance_criteria>
  <action>
    Create bash script with set -euo pipefail, strip trailing slashes from base URLs, helper check_http_200 and check_redirect_sample (reuse normalize pattern from verify-phase4-vercel.sh). Run redirect samples against VZHURUDOLU_URL for first 5 non-comment lines in redirect-samples.txt. Use curl with --max-time 20. Print Phase 9 cutover gate: PASS on success.
  </action>
  <verify>
    <automated>test -x scripts/verify-phase9-cutover.sh && bash -n scripts/verify-phase9-cutover.sh && grep -q 'VZHURUDOLU_URL' scripts/verify-phase9-cutover.sh && grep -q 'MICHALEK_DEV_URL' scripts/verify-phase9-cutover.sh</automated>
  </verify>
  <done>verify-phase9-cutover.sh committed and syntax-valid</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/verify-phase9-cutover.sh` — production URL smoke tests with env var overrides
