---
phase: 08-launch-polish-legal-feeds-tags-performance
plan: 05
type: execute
wave: 3
depends_on:
  - "08-04"
files_modified:
  - apps/michalek-dev/src/components/GoogleAnalytics.astro
  - packages/shared/layouts/BaseLayout.astro
  - apps/michalek-dev/src/env.d.ts
  - apps/michalek-dev/astro.config.mjs
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
user_setup:
  - service: Google Analytics 4
    why: "Deferred analytics measurement on michalek.dev"
    env_vars:
      - name: PUBLIC_GA_MEASUREMENT_ID
        source: "GA4 Admin → Data Streams → Web → Measurement ID (G-XXXXXXXX)"
    dashboard_config:
      - task: "Create GA4 property for michalek.dev if not exists"
        location: "Google Analytics Admin"

must_haves:
  truths:
    - "GA4 loads only after cookie consent accepted"
    - "Analytics script is async/deferred and does not block render"
    - "No render-blocking third-party scripts in EN dist"
    - "Images use existing Sharp/WebP pipeline (no regression)"
  artifacts:
    - path: apps/michalek-dev/src/components/GoogleAnalytics.astro
      provides: "Consent-gated GA4 loader"
      contains: "cookie-consent-updated"
  key_links:
    - from: GoogleAnalytics.astro
      to: CookieConsent localStorage
      via: "reads michalek-cookie-consent before inject"
      pattern: "michalek-cookie-consent"
    - from: packages/shared/layouts/BaseLayout.astro
      to: GoogleAnalytics.astro
      via: "EN-only slot after body content"
      pattern: "GoogleAnalytics"
---

<objective>
Integrate consent-gated deferred GA4 and verify pragmatic performance posture (PERF-01–04).

Purpose: PROJECT.md allows GA with pragmatic tradeoffs; must not block CWV or load without consent.
Output: GoogleAnalytics.astro, env var wiring, performance audit notes.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-launch-polish-legal-feeds-tags-performance/08-CONTEXT.md
@.planning/PROJECT.md
@packages/shared/components/CookieConsent.astro
@apps/michalek-dev/astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consent-gated GA4 component</name>
  <files>apps/michalek-dev/src/components/GoogleAnalytics.astro, apps/michalek-dev/src/env.d.ts</files>
  <action>
    Create GoogleAnalytics.astro loaded only on EN site. Read PUBLIC_GA_MEASUREMENT_ID from import.meta.env (add to env.d.ts Interface ImportMetaEnv). If env var empty at build, component renders nothing (dev-friendly). Inline script: on load check localStorage michalek-cookie-consent === 'accepted'; if yes call loadGA(). loadGA injects gtag.js via createElement async=true, defines dataLayer, gtag config with anonymize_ip true. Listen for cookie-consent-updated event from CookieConsent to load on Accept. Never load on Reject or missing consent. PERF-04: script tag async defer pattern; inject after DOMContentLoaded or idle — not in head blocking parse.
  </action>
  <verify>
    <automated>grep -q 'GoogleAnalytics' apps/michalek-dev/src/components/GoogleAnalytics.astro && grep -q 'async' apps/michalek-dev/src/components/GoogleAnalytics.astro && grep -q 'cookie-consent-updated' apps/michalek-dev/src/components/GoogleAnalytics.astro</automated>
  </verify>
  <done>GA component gates on consent and uses async script injection.</done>
</task>

<task type="auto">
  <name>Task 2: Wire GA into BaseLayout for EN only</name>
  <files>packages/shared/layouts/BaseLayout.astro</files>
  <action>
    Import GoogleAnalytics in BaseLayout or accept slot — prefer conditional import in BaseLayout when config.id === 'michalek-dev' to avoid CS loading. Place before closing body, after CookieConsent. Do not add GA to head. Rebuild both apps; confirm CS dist has no gtag references.
  </action>
  <verify>
    <automated>npm run build -w @vd/michalek-dev && npm run build -w @vd/vzhurudolu && ! grep -rq 'googletagmanager' apps/vzhurudolu/dist 2>/dev/null && grep -q 'cookie-consent-updated' apps/michalek-dev/dist/index.html</automated>
  </verify>
  <done>EN dist has GA loader stub; CS dist has no GA scripts.</done>
</task>

<task type="auto">
  <name>Task 3: Performance review gate checks</name>
  <files>apps/michalek-dev/astro.config.mjs</files>
  <action>
    PERF-01/02/03 verification pass (no Lighthouse gate per CONTEXT): (1) grep EN dist for render-blocking external scripts in head — must be zero third-party script src in head except none expected. (2) Confirm vitePluginContentImages still active in astro.config (PERF-03 Sharp pipeline). (3) Document in commit/plan summary: mobile CWV spot-check recommendation (homepage + one article) using Chrome DevTools or PageSpeed Insights — aspirational not blocking. Fix only if obvious regression found (e.g. sync script in head). Do not add performance colophon badge.
  </action>
  <verify>
    <automated>grep -q 'vitePluginContentImages' apps/michalek-dev/astro.config.mjs && npm run build -w @vd/michalek-dev && ! grep -E '<script[^>]+src="https?://' apps/michalek-dev/dist/index.html 2>/dev/null | grep -v cookie-consent || true</automated>
  </verify>
  <done>No blocking third-party head scripts; Sharp plugin confirmed active.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → GA | Analytics only after explicit consent |
| PUBLIC_GA_MEASUREMENT_ID | Non-secret public measurement ID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Privacy | GA before consent | mitigate | localStorage check + event listener |
| T-08-08 | DoS | Third-party script | accept | Single GA property; async load |
| T-08-SC | Tampering | npm installs | accept | No new packages; gtag loaded from Google CDN |
</threat_model>

<verification>
EN build has consent-gated GA stub; CS has no GA; no blocking external head scripts; content images plugin active.
</verification>

<success_criteria>
PERF-01–04 satisfied: deferred GA, no blocking widgets, Sharp pipeline intact.
</success_criteria>

<output>
Create `.planning/phases/08-launch-polish-legal-feeds-tags-performance/08-05-SUMMARY.md` when done
</output>
