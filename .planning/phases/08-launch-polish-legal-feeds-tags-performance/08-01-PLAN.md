---
phase: 08-launch-polish-legal-feeds-tags-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/seo/json-ld.ts
  - packages/shared/package.json
  - packages/shared/layouts/BaseLayout.astro
autonomous: true
requirements:
  - SEO-02
  - SEO-06
  - SEO-07
user_setup: []

must_haves:
  truths:
    - "BaseLayout supports EN Twitter Card, RSS head link, and JSON-LD injection"
    - "Person and Article JSON-LD builders include sameAs for LinkedIn and pagespeed.one"
  artifacts:
    - path: packages/shared/seo/json-ld.ts
      provides: "Person and Article schema builders"
      exports: ["buildPersonJsonLd", "buildArticleJsonLd"]
    - path: packages/shared/layouts/BaseLayout.astro
      provides: "EN Twitter Card, RSS link, jsonLd prop"
      contains: "twitter:card"
  key_links:
    - from: packages/shared/layouts/BaseLayout.astro
      to: packages/shared/seo/json-ld.ts
      via: "consumers pass jsonLd prop built from helpers"
      pattern: "jsonLd"
---

<objective>
Create shared SEO infrastructure: JSON-LD builders and BaseLayout EN social/structured-data hooks (SEO-02, SEO-06, SEO-07 foundation).

Purpose: Downstream plans wire pages against stable BaseLayout contract without parallel file conflicts.
Output: json-ld.ts, package export, BaseLayout EN Twitter Card + RSS link + jsonLd slot.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-launch-polish-legal-feeds-tags-performance/08-CONTEXT.md
@packages/shared/layouts/BaseLayout.astro
@apps/michalek-dev/src/site.config.ts
@apps/michalek-dev/src/pages/martin.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JSON-LD schema builders</name>
  <files>packages/shared/seo/json-ld.ts, packages/shared/package.json</files>
  <action>
    Create packages/shared/seo/json-ld.ts exporting buildPersonJsonLd() and buildArticleJsonLd({ title, description, url, datePublished, image? }). Person per SEO-07: @type Person, name Martin Michálek, url https://michalek.dev/martin, sameAs with https://www.linkedin.com/in/martinmichalek and https://pagespeed.one/. Article: @type Article with author Person, mainEntityOfPage, datePublished ISO. Add "./seo/json-ld" export to packages/shared/package.json. No new npm packages.
  </action>
  <verify>
    <automated>grep -q 'buildPersonJsonLd' packages/shared/seo/json-ld.ts && grep -q 'seo/json-ld' packages/shared/package.json</automated>
  </verify>
  <done>json-ld.ts and package export exist with sameAs URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Extend BaseLayout for EN Twitter, RSS, JSON-LD slot</name>
  <files>packages/shared/layouts/BaseLayout.astro</files>
  <action>
    Add optional jsonLd prop to BaseLayout Props. For config.locale === 'en': render twitter:card summary_large_image, twitter:title, twitter:description, twitter:image from og values. Add link rel="alternate" type="application/rss+xml" href="/rss" for EN. When jsonLd provided, emit script type="application/ld+json" with JSON.stringify. Keep html lang={config.locale}. Preserve all isCs-guarded CS meta unchanged. No GA scripts.
  </action>
  <verify>
    <automated>grep -q 'twitter:card' packages/shared/layouts/BaseLayout.astro && grep -q 'jsonLd' packages/shared/layouts/BaseLayout.astro && npm run build -w @vd/vzhurudolu 2>&1 | tail -2 && npm run build -w @vd/michalek-dev 2>&1 | tail -2</automated>
  </verify>
  <done>Both apps build; BaseLayout has EN Twitter, RSS link, jsonLd slot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time JSON-LD | Trusted server-side schema only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | json-ld content | mitigate | JSON.stringify server-built objects only |
| T-08-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
Both apps build after BaseLayout changes.
</verification>

<success_criteria>
Shared SEO infrastructure ready for page wiring in plans 08-02 and 08-03.
</success_criteria>

<output>
Create `.planning/phases/08-launch-polish-legal-feeds-tags-performance/08-01-SUMMARY.md` when done
</output>
