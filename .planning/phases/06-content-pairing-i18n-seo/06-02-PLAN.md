---
phase: 06-content-pairing-i18n-seo
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - packages/shared/seo/hreflang.ts
  - packages/shared/components/LanguageSwitch.astro
  - packages/shared/components/ArticleHeader.astro
  - packages/shared/layouts/BaseLayout.astro
  - packages/shared/package.json
autonomous: true
requirements:
  - I18N-03
  - I18N-05
  - I18N-06
user_setup: []

must_haves:
  truths:
    - "Paired pages can inject en, cs, and x-default hreflang link tags with absolute URLs"
    - "Every page using BaseLayout can emit self-referencing link rel=canonical"
    - "LanguageSwitch shows current locale as plain text and alternate locale as link with native labels"
  artifacts:
    - path: packages/shared/seo/hreflang.ts
      provides: "buildAlternateLinks helper"
      exports: ["buildAlternateLinks"]
    - path: packages/shared/components/LanguageSwitch.astro
      provides: "Cross-domain language switch UI"
      contains: "Česky"
    - path: packages/shared/layouts/BaseLayout.astro
      provides: "canonical and hreflang injection"
      contains: 'rel="canonical"'
  key_links:
    - from: packages/shared/layouts/BaseLayout.astro
      to: packages/shared/seo/hreflang.ts
      via: "alternateLinks prop"
      pattern: "buildAlternateLinks|hreflang"
    - from: packages/shared/components/LanguageSwitch.astro
      to: alternate href
      via: "anchor href prop"
      pattern: "href="
---

<objective>
Add shared SEO helpers and LanguageSwitch UI — hreflang cluster builder, self-referencing canonical in BaseLayout, and article-header placement (I18N-03, I18N-05, I18N-06).

Purpose: Gives both apps a consistent head-tag and switch component before page wiring in Plans 03–04.
Output: buildAlternateLinks(), LanguageSwitch.astro, BaseLayout alternateLinks + canonical support, ArticleHeader language switch slot.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-content-pairing-i18n-seo/06-CONTEXT.md
@packages/shared/layouts/BaseLayout.astro
@packages/shared/components/ArticleHeader.astro
@packages/shared/content-pairing/resolve.ts
@.planning/phases/05-english-app-scaffold/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: hreflang and canonical SEO helpers</name>
  <files>packages/shared/seo/hreflang.ts, packages/shared/package.json</files>
  <action>
    Create packages/shared/seo/hreflang.ts exporting buildAlternateLinks({ csUrl, enUrl, xDefaultUrl }) returning array of { hreflang, href } for en, cs, x-default where xDefaultUrl equals enUrl per I18N-05 and 06-CONTEXT D-03. Add export "./seo/hreflang" to package.json. Helper is pure — no Astro imports. URLs must be passed absolute; do not prepend origins inside helper.
  </action>
  <verify>
    <automated>node --input-type=module -e "import { buildAlternateLinks } from './packages/shared/seo/hreflang.ts'; const links = buildAlternateLinks({ csUrl: 'https://www.vzhurudolu.cz/prirucka/webp', enUrl: 'https://michalek.dev/guide/webp', xDefaultUrl: 'https://michalek.dev/guide/webp' }); if (links.length !== 3) throw new Error('expected 3'); if (!links.some(l => l.hreflang === 'x-default' && l.href.includes('michalek.dev'))) throw new Error('x-default'); console.log('ok')"</automated>
  </verify>
  <done>buildAlternateLinks returns three absolute hreflang entries with x-default pointing to EN</done>
</task>

<task type="auto">
  <name>Task 2: BaseLayout canonical and hreflang injection</name>
  <files>packages/shared/layouts/BaseLayout.astro</files>
  <action>
    Extend BaseLayout Props with optional alternateLinks?: Array of hreflang href objects from buildAlternateLinks. When canonicalUrl prop provided (or derive from ogUrl), emit link rel=canonical href={finalCanonicalUrl} in head before og tags per I18N-06 — canonical must always be self-referencing current page, never cross-language. When alternateLinks provided, map to link rel=alternate hreflang={hreflang} href={href} elements in head (paired pages only per D-03). Do not remove existing CS legacy cssmine hreflang link — it is unrelated to pairing and out of scope. Do not add hreflang on pages where alternateLinks prop is omitted. Ensure EN pages omit cssmine block (existing isCs guard). og:url continues to use finalCanonicalUrl.
  </action>
  <verify>
    <automated>npm run build -w @vd/vzhurudolu && npm run build -w @vd/michalek-dev</automated>
  </verify>
  <done>Both apps still build; BaseLayout accepts alternateLinks and emits rel=canonical when canonicalUrl set</done>
</task>

<task type="auto">
  <name>Task 3: LanguageSwitch component and ArticleHeader slot</name>
  <files>packages/shared/components/LanguageSwitch.astro, packages/shared/components/ArticleHeader.astro</files>
  <action>
    Create LanguageSwitch.astro per 06-CONTEXT D-02: Props currentLocale ('cs'|'en'), alternateUrl (string|undefined), optional className. When alternateUrl missing, render nothing (hidden on unpaired pages). When present: render nav or p with class language-switch using existing VD utility classes (e.g. page-subhead meta styling — match page-subhead__meta tone). Current locale shown as plain text with native label: cs → Česky, en → English. Alternate locale as anchor linking to alternateUrl with rel=alternate hreflang on the anchor optional (head tags are primary). Labels exactly English and Česky per I18N-03. Extend ArticleHeader with optional languageSwitch slot rendered below page-subhead block (below title/meta row) per D-02 placement. Do not show switch in Header nav — article header area only.
  </action>
  <verify>
    <automated>npm run build -w @vd/michalek-dev && grep -q 'LanguageSwitch\|language-switch' packages/shared/components/LanguageSwitch.astro</automated>
  </verify>
  <done>LanguageSwitch component exists; ArticleHeader accepts languageSwitch slot below meta</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML head output | hreflang and canonical tags consumed by search engines — must not cross-wire domains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | BaseLayout canonical | mitigate | canonicalUrl always current page absolute URL; verify script greps dist for cross-domain canonical absence |
| T-06-04 | Spoofing | LanguageSwitch alternateUrl | mitigate | alternateUrl sourced only from resolveAlternate in page templates, not user input |
| T-06-SC | Tampering | npm installs | accept | No new deps |
</threat_model>

<verification>
buildAlternateLinks unit check passes; dual-app build succeeds after BaseLayout and ArticleHeader changes.
</verification>

<success_criteria>
Shared components ready for page wiring; canonical link tag available; LanguageSwitch hidden when no alternateUrl.
</success_criteria>

<output>
Create `.planning/phases/06-content-pairing-i18n-seo/06-02-SUMMARY.md` when done
</output>
