---
phase: 06-content-pairing-i18n-seo
plan: 05
type: execute
wave: 4
depends_on:
  - "06-03"
  - "06-04"
files_modified:
  - packages/shared/content-pairing/validate.ts
  - scripts/validate-content-pairs.mjs
  - scripts/verify-phase6-content-pairing-i18n-seo.sh
  - .github/workflows/pr-build.yml
  - package.json
autonomous: true
requirements:
  - I18N-04
  - I18N-07
user_setup: []

must_haves:
  truths:
    - "Build fails when CONTENT_PAIRS has orphan or non-reciprocal entry"
    - "Verify gate asserts hreflang and language switch on paired dist HTML for both apps"
    - "No auto language redirect exists in configs or middleware"
    - "PR CI runs Phase 6 verify gate after dual-app build"
  artifacts:
    - path: scripts/verify-phase6-content-pairing-i18n-seo.sh
      provides: "Phase 6 automated gate"
      contains: "Phase 6"
    - path: scripts/validate-content-pairs.mjs
      provides: "CLI validator with exit code 1 on errors"
      contains: "validateContentPairs"
    - path: .github/workflows/pr-build.yml
      provides: "CI integration"
      contains: "verify-phase6"
  key_links:
    - from: scripts/verify-phase6-content-pairing-i18n-seo.sh
      to: apps/vzhurudolu/dist and apps/michalek-dev/dist
      via: "grep hreflang and LanguageSwitch labels"
      pattern: "hreflang"
    - from: scripts/validate-content-pairs.mjs
      to: packages/shared/content-pairing/validate.ts
      via: "process.exit(1) on !ok"
      pattern: "validateContentPairs"
---

<objective>
Complete build-time pairing validation, Phase 6 verify gate, and CI integration — fail build on broken pairs; confirm no auto-redirect (I18N-04, I18N-07).

Purpose: Closes phase with automated enforcement matching ROADMAP success criteria #5 and #6.
Output: verify-phase6-content-pairing-i18n-seo.sh PASS; validate-content-pairs.mjs fails on injected orphan pair.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-content-pairing-i18n-seo/06-CONTEXT.md
@scripts/verify-phase5-english-scaffold.sh
@scripts/validate-content-pairs.mjs
@packages/shared/content-pairing/validate.ts
@.github/workflows/pr-build.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Full validator with frontmatter cross-check and fail-fast CLI</name>
  <files>packages/shared/content-pairing/validate.ts, scripts/validate-content-pairs.mjs</files>
  <behavior>
    - validateContentPairs with duplicate pairId returns ok false
    - validateContentPairs with frontmatter pairId mismatch vs manifest key returns error
    - CLI exits 1 when errors present, 0 when clean
    - Missing EN content file emits warning not error (Phase 7 tolerance per D-04)
  </behavior>
  <action>
    Extend validate.ts per I18N-04 and 06-CONTEXT D-04: accept optional frontmatterIndex Map of pairId→{site,collection,key} scraped by CLI from both apps content dirs; error when front matter pairId not in manifest or manifest key mismatch; error on orphan/non-reciprocal pairs; warn when EN markdown file for paired guide/blog key missing under apps/michalek-dev/src/content (do not fail). validate-content-pairs.mjs: walk CS prirucka/blog and EN guide/blog markdown for pairId YAML; invoke validateContentPairs; process.exit(1) on errors. Add npm script validate:pairs at repo root if missing. Document that astro build does not auto-run validator — verify gate runs it before dist assertions.
  </action>
  <verify>
    <automated>node scripts/validate-content-pairs.mjs; node --input-type=module -e "import { validateContentPairs, CONTENT_PAIRS } from './packages/shared/content-pairing/index.ts'; const bad = [...CONTENT_PAIRS, { pairId: 'orphan-test', cs: { site: 'vzhurudolu', collection: 'prirucka', key: 'fake' }, en: { site: 'michalek-dev', collection: 'guide', key: 'fake' } }]; if (validateContentPairs(bad).ok) throw new Error('should fail orphan'); console.log('ok')"</automated>
  </verify>
  <done>Validator CLI exits 0 on real manifest; injected orphan pair fails validation</done>
</task>

<task type="auto">
  <name>Task 2: Phase 6 verify gate script</name>
  <files>scripts/verify-phase6-content-pairing-i18n-seo.sh</files>
  <action>
    Create scripts/verify-phase6-content-pairing-i18n-seo.sh following verify-phase5 pattern: run node scripts/validate-content-pairs.mjs; npm run build both apps; assert CS dist prirucka/webp and blog/261-rok-2025 contain hreflang en and cs, x-default to michalek.dev, rel canonical to vzhurudolu.cz, English language switch link; assert EN dist guide/webp and blog/2025-year-in-review contain hreflang trio, canonical to michalek.dev, Česky link to vzhurudolu.cz; assert both martin/index.html have language switch; assert unpaired CS page (e.g. podcast index if exists, or homepage) lacks hreflang="en" pairing cluster — use homepage index.html must NOT contain michalek.dev/guide pattern; grep -v for Accept-Language redirect in both vercel.json files and astro configs (I18N-07). Extend prior phase gate optionally by calling verify-phase5 at start or document standalone — prefer standalone dual build for speed. Exit non-zero on any failure with clear message.
  </action>
  <verify>
    <automated>bash scripts/verify-phase6-content-pairing-i18n-seo.sh</automated>
  </verify>
  <done>Verify script PASS on current tree after Plans 03–04 executed</done>
</task>

<task type="auto">
  <name>Task 3: PR CI integration</name>
  <files>.github/workflows/pr-build.yml, package.json</files>
  <action>
    Add bash scripts/verify-phase6-content-pairing-i18n-seo.sh to pr-build.yml after verify-phase5-english-scaffold.sh step. Add root package.json script verify:phase6 pointing to the shell script if useful for local dev. Do not remove existing verify steps.
  </action>
  <verify>
    <automated>grep -q 'verify-phase6-content-pairing-i18n-seo' .github/workflows/pr-build.yml</automated>
  </verify>
  <done>PR CI workflow references Phase 6 verify gate</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI verify gate | Last line of defense before merge — dist HTML must match pairing contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering | CONTENT_PAIRS manifest | mitigate | validate-content-pairs.mjs exit 1 in CI |
| T-06-10 | Denial of service | Verify script | accept | Script completes under 5 min on CI — dual build only |
| T-06-SC | Tampering | npm installs | accept | No new deps |
</threat_model>

<verification>
bash scripts/verify-phase6-content-pairing-i18n-seo.sh exits 0; orphan pair injection test fails validator.
</verification>

<success_criteria>
I18N-04 build-time reciprocity enforced; I18N-07 no auto-redirect confirmed; CI runs Phase 6 gate.
</success_criteria>

<output>
Create `.planning/phases/06-content-pairing-i18n-seo/06-05-SUMMARY.md` when done
</output>
