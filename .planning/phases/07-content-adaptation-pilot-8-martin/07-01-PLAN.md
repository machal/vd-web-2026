---
phase: 07-content-adaptation-pilot-8-martin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/michalek-dev/src/content/config.ts
  - packages/shared/components/AdaptationAttribution.astro
  - apps/michalek-dev/src/pages/guide/[slug].astro
  - apps/michalek-dev/src/pages/blog/[slug].astro
autonomous: true
requirements:
  - CONT-03
  - CONT-04
  - CONT-05
user_setup: []

must_haves:
  truths:
    - "Guide and blog schemas accept adaptedFrom attribution metadata and guide author field"
    - "Every EN article page renders author byline linking to /martin"
    - "Every EN article page renders attribution footer when adaptedFrom is set"
  artifacts:
    - path: apps/michalek-dev/src/content/config.ts
      provides: "adaptedFrom + guide author schema fields"
      contains: "adaptedFrom"
    - path: packages/shared/components/AdaptationAttribution.astro
      provides: "Reusable attribution footer component"
      min_lines: 10
    - path: apps/michalek-dev/src/pages/guide/[slug].astro
      provides: "Guide author + attribution wiring"
      contains: "AdaptationAttribution"
  key_links:
    - from: apps/michalek-dev/src/pages/guide/[slug].astro
      to: packages/shared/components/ArticleHeader.astro
      via: "author prop with locale en"
      pattern: "author="
    - from: apps/michalek-dev/src/pages/blog/[slug].astro
      to: packages/shared/components/AdaptationAttribution.astro
      via: "post.data.adaptedFrom"
      pattern: "AdaptationAttribution"
---

<objective>
Establish EN article adaptation contracts — schema fields, author byline on guide pages, and rendered attribution footer — so parallel article plans can focus on content only (CONT-03, CONT-04, CONT-05).

Purpose: Phase 6 stubs lack author on guides and attribution; this plan defines the shared interface all 8 adaptations must use per D-01 (adaptation workflow).
Output: Extended content schema, AdaptationAttribution component, wired blog/guide article templates.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-content-adaptation-pilot-8-martin/07-CONTEXT.md
@.cursor/skills/vd-articles-write-edit/SKILL.md
@apps/michalek-dev/src/content/config.ts
@apps/michalek-dev/src/pages/guide/[slug].astro
@apps/michalek-dev/src/pages/blog/[slug].astro
@packages/shared/components/ArticleHeader.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend EN content schema for adaptation metadata</name>
  <files>apps/michalek-dev/src/content/config.ts</files>
  <action>
    Per D-01, add shared front matter fields to both blog and guide collections in apps/michalek-dev/src/content/config.ts. Add author to guide schema (z.string().optional().default('Martin Michálek') — mirror blog). Add adaptedFrom as z.object({ title: z.string(), url: z.string().url() }).optional() on both collections. Keep existing pairId, title, description, date, published, tags fields unchanged. Do not add Czech-only fields (postTitle, perex legacy transforms stay as-is on guide). Export types are inferred by Astro — no separate types file needed.
  </action>
  <verify>
    <automated>npm run build -w @vd/michalek-dev 2>&1 | tail -5</automated>
  </verify>
  <done>Both collections accept author and adaptedFrom in front matter; build passes with existing stubs.</done>
</task>

<task type="auto">
  <name>Task 2: Create AdaptationAttribution component and wire article templates</name>
  <files>packages/shared/components/AdaptationAttribution.astro, apps/michalek-dev/src/pages/guide/[slug].astro, apps/michalek-dev/src/pages/blog/[slug].astro</files>
  <action>
    Create packages/shared/components/AdaptationAttribution.astro accepting props adaptedFrom: { title: string; url: string } and optional locale defaulting to en. Render a footer block after article body (before ArticleFooter) using VD article-foot styling pattern: small italic paragraph "Adapted from the Czech article [Title](url) on Vzhůru dolů." — exact wording per D-01 attribution footer decision. Wire guide/[slug].astro: pass author={post.data.author ?? 'Martin Michálek'} and date to ArticleHeader (blog already passes author — keep parity). Render AdaptationAttribution when post.data.adaptedFrom is defined, placed inside content div after Content slot. Wire blog/[slug].astro identically for attribution. ArticleHeader already links author to /martin — no change needed there (CONT-04). Do not modify CS app files.
  </action>
  <verify>
    <automated>grep -q 'AdaptationAttribution' apps/michalek-dev/src/pages/guide/\[slug\].astro && grep -q 'author=' apps/michalek-dev/src/pages/guide/\[slug\].astro && npm run build -w @vd/michalek-dev 2>&1 | tail -3</automated>
  </verify>
  <done>Guide pages show author byline; both templates render AdaptationAttribution when adaptedFrom present; build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown content → static HTML | Adapted article body rendered at build time; no runtime user input |
| adaptedFrom.url → outbound link | External URL from front matter; must be https vzhurudolu.cz canonical |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | adaptedFrom.url in front matter | mitigate | Zod .url() validation in content schema rejects malformed URLs at build |
| T-07-02 | Spoofing | Attribution link target | accept | Static content authored by maintainer; no user-generated adaptedFrom |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
npm run build -w @vd/michalek-dev passes; guide template passes author to ArticleHeader; AdaptationAttribution component exists and is imported in both article templates.
</verification>

<success_criteria>
Schema and templates ready for 8 parallel article adaptations — author byline and attribution footer infrastructure complete.
</success_criteria>

<output>
Create `.planning/phases/07-content-adaptation-pilot-8-martin/07-01-SUMMARY.md` when done
</output>
