---
phase: 01-monorepo-foundation-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - apps/vzhurudolu/package.json
  - apps/vzhurudolu/astro.config.mjs
  - apps/vzhurudolu/tsconfig.json
  - apps/vzhurudolu/vite-plugin-content-images.ts
  - apps/vzhurudolu/vite-plugin-design-images.ts
  - apps/vzhurudolu/vite-plugin-prirucka-images.ts
  - apps/vzhurudolu/vite-plugin-validate-frontmatter.ts
  - apps/vzhurudolu/src/
  - apps/vzhurudolu/public/
  - apps/vzhurudolu/scripts/
  - apps/michalek-dev/package.json
  - packages/.gitkeep
  - scripts/verify-phase1-build.sh
autonomous: true
requirements:
  - MONO-01
user_setup: []

must_haves:
  truths:
    - "Developer can npm install at repo root without workspace resolution errors"
    - "Czech Astro site builds from apps/vzhurudolu and writes dist/ inside the app directory"
    - "Workspace contains apps/vzhurudolu, apps/michalek-dev placeholder, and packages/ directory"
    - "D-22: Czech site build completes without errors after monorepo move (URL parity verification deferred to Phase 3)"
    - "D-21: Monorepo restructure executed on michalek-dev branch per PROJECT.md"
  artifacts:
    - path: apps/vzhurudolu/package.json
      provides: "@vd/vzhurudolu app package with astro 4.16.19 and all runtime deps"
      contains: '"name": "@vd/vzhurudolu"'
    - path: apps/michalek-dev/package.json
      provides: "MONO-01 placeholder workspace member"
      contains: '"name": "@vd/michalek-dev"'
    - path: packages/.gitkeep
      provides: "Empty packages directory per D-04"
    - path: scripts/verify-phase1-build.sh
      provides: "Phase 1 build smoke gate"
      min_lines: 10
  key_links:
    - from: package.json
      to: apps/vzhurudolu/package.json
      via: npm workspaces array
      pattern: '"workspaces".*"apps/\\*"'
    - from: apps/vzhurudolu/package.json
      to: apps/vzhurudolu/dist/
      via: npm run build in app directory
      pattern: '"build".*"astro build"'
---

## Phase Goal

**As a** developer maintaining vzhurudolu.cz, **I want to** build the Czech site from the monorepo with CI guardrails, **so that** structural changes do not break production deploys or leak draft content.

<objective>
Deliver the walking-skeleton vertical slice: npm workspaces layout with the entire Czech codebase relocated to `apps/vzhurudolu` and a successful local build producing `apps/vzhurudolu/dist/`.

Purpose: Establishes MONO-01 workspace structure (D-01, D-04) — the thinnest end-to-end path from repo root install to Czech SSG output.
Output: Relocated app tree, workspace scaffold, build verification script, passing `npm run build -w @vd/vzhurudolu`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-monorepo-foundation-ci/01-SKELETON.md
@.planning/phases/01-monorepo-foundation-ci/01-CONTEXT.md
@.planning/phases/01-monorepo-foundation-ci/01-RESEARCH.md
@.planning/phases/01-monorepo-foundation-ci/01-PATTERNS.md
@package.json
@astro.config.mjs
@.cursor/rules/astro-build.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace skeleton and build verification gate</name>
  <read_first>
    - .planning/phases/01-monorepo-foundation-ci/01-CONTEXT.md (D-04, D-05, D-12)
    - .planning/phases/01-monorepo-foundation-ci/01-RESEARCH.md (Recommended Project Structure)
    - package.json (current root scripts and deps to split later)
  </read_first>
  <files>
    apps/michalek-dev/package.json
    packages/.gitkeep
    scripts/verify-phase1-build.sh
  </files>
  <action>
    Create MONO-01 workspace skeleton per D-04 and D-05 before any file moves.

    1. Create `apps/michalek-dev/package.json` with exactly: `"name": "@vd/michalek-dev"`, `"private": true`, `"version": "0.0.0"`. Do NOT add `build`, `dev`, or `astro` scripts (D-12).

    2. Create `packages/.gitkeep` (empty packages directory).

    3. Create `scripts/verify-phase1-build.sh` (executable) as the Phase 1 end-to-end gate. Script must:
       - Exit non-zero if `apps/vzhurudolu/dist/` is missing
       - Exit non-zero if `apps/vzhurudolu/dist/_astro/` is missing or empty (per astro-build.md rule)
       - Exit 0 with message "Phase 1 build gate: dist OK" when checks pass
       - Include commented placeholders for VD-02 draft smoke checks (Plan 03 will activate them)

    Run the script once before scaffold is complete — it MUST fail (RED gate for walking skeleton).
  </action>
  <acceptance_criteria>
    - `apps/michalek-dev/package.json` exists with name `@vd/michalek-dev` and no build script
    - `packages/.gitkeep` exists
    - `scripts/verify-phase1-build.sh` is executable and fails when dist absent
    - Script checks `apps/vzhurudolu/dist/_astro/` per astro-build.md
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/verify-phase1-build.sh; test $? -ne 0</automated>
  </verify>
  <done>Placeholder app and packages dir exist; build verification script fails as expected before migration.</done>
</task>

<task type="auto">
  <name>Task 2: Big-bang relocate Czech codebase to apps/vzhurudolu</name>
  <read_first>
    - .planning/phases/01-monorepo-foundation-ci/01-RESEARCH.md (Exact File Move Inventory)
    - .planning/phases/01-monorepo-foundation-ci/01-PATTERNS.md (vite-plugin cwd contract)
    - astro.config.mjs
    - vite-plugin-prirucka-images.ts (process.cwd() usage)
  </read_first>
  <files>
    apps/vzhurudolu/astro.config.mjs
    apps/vzhurudolu/vite-plugin-content-images.ts
    apps/vzhurudolu/vite-plugin-design-images.ts
    apps/vzhurudolu/vite-plugin-prirucka-images.ts
    apps/vzhurudolu/vite-plugin-validate-frontmatter.ts
    apps/vzhurudolu/src/
    apps/vzhurudolu/public/
    apps/vzhurudolu/scripts/
    apps/vzhurudolu/tsconfig.json
  </files>
  <action>
    Execute big-bang move per D-01 and D-02 — no half-migrated dual-root state.

    MOVE to `apps/vzhurudolu/` using git mv where tracked:
    - `src/` (entire tree)
    - `public/` (entire tree, ~685 MB)
    - `astro.config.mjs`
    - All four `vite-plugin-*.ts` files at repo root
    - `scripts/` (all maintenance scripts)

    DO NOT move: `.github/`, `.planning/`, `.cursor/`, `README.md`, `docs/`, root `favicon/` duplicate, `grunt/`, `.gitignore`, `.gitleaks.toml`, `.stylelintrc`.

    After move, delete stale root artifacts if present: root `dist/`, root `.astro/` cache.

    Create `apps/vzhurudolu/tsconfig.json` extending `"astro/tsconfigs/strict"` (recommended per RESEARCH open question — low cost for Phase 2).

    Do NOT change import paths inside moved files — relative imports assume cwd = `apps/vzhurudolu`.

    If root `.env` exists (gitignored), move to `apps/vzhurudolu/.env` if any code reads env vars; grep for `import.meta.env` first.

    Vite plugins and markdown utilities stay inside app per D-02 — do not create packages/* yet.
  </action>
  <acceptance_criteria>
    - No `src/`, `public/`, or `astro.config.mjs` remain at repo root
    - `apps/vzhurudolu/src/pages/index.astro` exists
    - All four vite-plugin files live under `apps/vzhurudolu/`
    - `apps/vzhurudolu/tsconfig.json` extends astro strict config
    - Root `dist/` removed if it existed
  </acceptance_criteria>
  <verify>
    <automated>test ! -e src/pages/index.astro && test -f apps/vzhurudolu/src/pages/index.astro && test -f apps/vzhurudolu/astro.config.mjs</automated>
  </verify>
  <done>Entire Czech Astro codebase lives under apps/vzhurudolu with unchanged relative imports.</done>
</task>

<task type="auto">
  <name>Task 3: App package.json, root workspaces, and first green build</name>
  <read_first>
    - package.json (current root — source for dep migration)
    - .planning/phases/01-monorepo-foundation-ci/01-PATTERNS.md (apps/vzhurudolu/package.json pattern)
    - astro.config.mjs imports (verify rehype-raw and remark-gfm are declared)
  </read_first>
  <files>
    apps/vzhurudolu/package.json
    package.json
    package-lock.json
  </files>
  <action>
    Split root package.json into workspace orchestrator (interim, pre-Turbo) and app package per D-06, D-07, D-09.

    **apps/vzhurudolu/package.json:**
    - `"name": "@vd/vzhurudolu"`
    - Move ALL current root `dependencies` and `devDependencies` here
    - Pin `"astro": "4.16.19"` exactly (D-06)
    - Keep scripts: `dev`, `build`, `preview`, `astro`, `convert-images`, `fill-heading`, `check-frontmatter`, `fix-frontmatter`
    - Audit `astro.config.mjs` imports — add any missing deps (`rehype-raw`, etc.) to app package.json

    **Root package.json (interim — Turbo added in Plan 02):**
    - `"name": "vd-monorepo"`, `"private": true`
    - `"workspaces": ["apps/*", "packages/*"]`
    - `"engines": { "node": ">=22" }`
    - Scripts: `"build:vzhurudolu": "npm run build -w @vd/vzhurudolu"`, `"dev:vzhurudolu": "npm run dev -w @vd/vzhurudolu"`
    - NO runtime deps at root (D-03) — defer `turbo`/`typescript` to Plan 02

    Run `npm install` at repo root to regenerate `package-lock.json` with workspace links (D-09).

    Build from app workspace: `npm run build -w @vd/vzhurudolu`.

    Run `bash scripts/verify-phase1-build.sh` — must pass (GREEN).

    Verify `ls apps/vzhurudolu/dist/_astro/` shows JS assets per astro-build.md.
  </action>
  <acceptance_criteria>
    - `@vd/vzhurudolu` declares `"astro": "4.16.19"` in its own package.json
    - Root package.json has workspaces and NO astro dependency
    - `npm ci` succeeds at repo root
    - `npm run build -w @vd/vzhurudolu` exits 0
    - `scripts/verify-phase1-build.sh` exits 0
    - `apps/vzhurudolu/dist/_astro/` contains files
  </acceptance_criteria>
  <verify>
    <automated>npm ci && npm run build -w @vd/vzhurudolu && bash scripts/verify-phase1-build.sh</automated>
  </verify>
  <done>MONO-01 satisfied: developer installs at root and builds Czech site from apps/vzhurudolu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → repo filesystem | File moves must not expose secrets or break git history |
| Build output → dist/ | Static artifacts must include _astro/ assets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | File move (git mv) | mitigate | Use git mv for tracked files; verify no credential files moved |
| T-01-02 | Information Disclosure | .env handling | mitigate | Move .env to app dir only if needed; never commit; keep gitignored |
| T-01-SC | Tampering | npm install (existing deps only) | accept | No new packages in Plan 01; existing lockfile deps already in use |

</threat_model>

<verification>
- `npm ci && npm run build -w @vd/vzhurudolu` passes
- `bash scripts/verify-phase1-build.sh` passes
- Root has no `src/` or `astro.config.mjs`
- Workspace members: `@vd/vzhurudolu`, `@vd/michalek-dev`, `packages/`
</verification>

<success_criteria>
- Walking skeleton proven: install → build → dist with _astro/ assets
- MONO-01 workspace layout complete with relocated Czech app
</success_criteria>

## Artifacts This Phase Produces

| Artifact | Path |
|----------|------|
| Czech app package | `apps/vzhurudolu/package.json` |
| Workspace root (interim) | `package.json`, `package-lock.json` |
| EN placeholder | `apps/michalek-dev/package.json` |
| Packages skeleton | `packages/.gitkeep` |
| Build gate script | `scripts/verify-phase1-build.sh` |
| Relocated Astro site | `apps/vzhurudolu/{src,public,scripts,astro.config.mjs,vite-plugin-*.ts}` |

<output>
Create `.planning/phases/01-monorepo-foundation-ci/01-01-SUMMARY.md` when done
</output>
