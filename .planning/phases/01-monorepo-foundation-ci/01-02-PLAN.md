---
phase: 01-monorepo-foundation-ci
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - package.json
  - package-lock.json
  - turbo.json
  - tsconfig.json
  - apps/vzhurudolu/package.json
autonomous: false
requirements:
  - MONO-02
  - MONO-04
user_setup: []

must_haves:
  truths:
    - "Developer can run turbo run build --filter=@vd/vzhurudolu from repo root"
    - "Turbo executes astro build with cwd = apps/vzhurudolu so Vite plugins resolve paths"
    - "astro and @astrojs/* dependencies exist only in apps/vzhurudolu/package.json, not root"
  artifacts:
    - path: turbo.json
      provides: "Turborepo task graph with build and dev tasks"
      contains: '"tasks"'
    - path: package.json
      provides: "Root orchestrator with turbo scripts only"
      contains: '"turbo"'
    - path: apps/vzhurudolu/package.json
      provides: "Self-contained Astro deps per MONO-04"
      contains: '"astro": "4.16.19"'
  key_links:
    - from: package.json
      to: turbo.json
      via: turbo run build script
      pattern: 'turbo run build'
    - from: turbo.json
      to: apps/vzhurudolu/dist/
      via: build task outputs dist/**
      pattern: '"outputs".*"dist/\\*\\*"'
---

## Phase Goal

**As a** developer maintaining vzhurudolu.cz, **I want to** build the Czech site from the monorepo with CI guardrails, **so that** structural changes do not break production deploys or leak draft content.

<objective>
Add Turborepo orchestration and finalize dependency isolation so filtered build commands work from repo root for local dev and CI (MONO-02, MONO-04).

Purpose: Completes the monorepo build pipeline per D-08, D-10, D-11, D-12 — turbo filter replaces interim npm -w commands.
Output: `turbo.json`, root orchestrator with turbo/typescript only, verified `npx turbo run build --filter=@vd/vzhurudolu`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-monorepo-foundation-ci/01-SKELETON.md
@.planning/phases/01-monorepo-foundation-ci/01-RESEARCH.md
@.planning/phases/01-monorepo-foundation-ci/01-PATTERNS.md
@.planning/phases/01-monorepo-foundation-ci/01-01-SUMMARY.md
@package.json
@apps/vzhurudolu/package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify turbo and typescript package legitimacy</name>
  <read_first>
    - .planning/phases/01-monorepo-foundation-ci/01-RESEARCH.md (Package Legitimacy Audit)
  </read_first>
  <files>package.json</files>
  <action>
    Package legitimacy gate per RESEARCH.md Package Legitimacy Audit before installing turbo@^2.9.16 and typescript@^5.9.3 at root.

    Human must verify:
    1. https://www.npmjs.com/package/turbo — publisher Vercel, repo github.com/vercel/turborepo, no suspicious postinstall
    2. https://www.npmjs.com/package/typescript — publisher Microsoft, repo github.com/microsoft/TypeScript
    3. Both tagged [ASSUMED] in audit — confirm acceptable to install

    Do NOT run npm install -D turbo until human approves. Resume signal required before Task 2.
  </action>
  <acceptance_criteria>
    - Human confirms turbo and typescript are legitimate npm packages from expected publishers
    - No [SLOP] verdict packages involved
  </acceptance_criteria>
  <what-built>Package legitimacy review for root devDependencies: turbo@^2.9.16 and typescript@^5.9.3</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/turbo — confirm publisher is Vercel, repository links to github.com/vercel/turborepo, no suspicious postinstall scripts
    2. Open https://www.npmjs.com/package/typescript — confirm publisher is Microsoft, repository links to github.com/microsoft/TypeScript
    3. Both packages marked [ASSUMED] in RESEARCH.md Package Legitimacy Audit — confirm acceptable to install
  </how-to-verify>
  <verify>
    <automated>grep -q 'ASSUMED' .planning/phases/01-monorepo-foundation-ci/01-RESEARCH.md</automated>
  </verify>
  <done>Human approved turbo and typescript install; executor may proceed to Task 2.</done>
  <resume-signal>Type "approved" to proceed with npm install -D turbo typescript</resume-signal>
</task>

<task type="auto">
  <name>Task 2: turbo.json and root orchestrator refactor</name>
  <read_first>
    - .planning/phases/01-monorepo-foundation-ci/01-RESEARCH.md (turbo.json pattern, D-10)
    - .planning/phases/01-monorepo-foundation-ci/01-PATTERNS.md (turbo.json section)
    - package.json (current interim workspace root from Plan 01)
  </read_first>
  <files>
    turbo.json
    package.json
    package-lock.json
    tsconfig.json
  </files>
  <action>
    Install Turborepo and TypeScript at root per D-08 after Task 1 approval:
    `npm install -D turbo@^2.9.16 typescript@^5.9.3`

    Create `turbo.json` at repo root per D-10:
    - Use `"tasks"` key (Turbo 2.x, NOT legacy `"pipeline"`)
    - `build` task: `"dependsOn": ["^build"]`, `"outputs": ["dist/**"]`
    - `dev` task: `"cache": false`, `"persistent": true`

    Refactor root `package.json` per D-03, D-08, D-11:
    - Replace interim `-w` scripts with turbo commands:
      - `"build": "turbo run build"`
      - `"dev": "turbo run dev"`
      - `"build:vzhurudolu": "turbo run build --filter=@vd/vzhurudolu"`
      - `"dev:vzhurudolu": "turbo run dev --filter=@vd/vzhurudolu"`
    - Root devDependencies ONLY: `turbo`, `typescript`
    - Keep `"engines": { "node": ">=22" }`
    - Remove any runtime deps accidentally left at root

    Create optional root `tsconfig.json` with `"files": []` or minimal base config for IDE support.

    Run `npm install` to update lockfile.

    Placeholder `@vd/michalek-dev` must still have NO build script (D-12) — turbo builds only `@vd/vzhurudolu`.
  </action>
  <acceptance_criteria>
    - turbo.json uses `tasks` key with build and dev tasks
    - Root package.json has turbo and typescript as only devDependencies
    - Root has zero astro or @astrojs/* dependencies
    - `@vd/michalek-dev` has no build script
    - `npx turbo run build --filter=@vd/vzhurudolu` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx turbo run build --filter=@vd/vzhurudolu && bash scripts/verify-phase1-build.sh</automated>
  </verify>
  <done>MONO-02 turbo pipeline operational; only Czech app builds.</done>
</task>

<task type="auto">
  <name>Task 3: MONO-04 dependency isolation audit</name>
  <read_first>
    - .planning/phases/01-monorepo-foundation-ci/01-RESEARCH.md (Pattern 2: App-Local Astro Dependencies)
    - apps/vzhurudolu/package.json
    - apps/vzhurudolu/astro.config.mjs (all import deps)
  </read_first>
  <files>
    apps/vzhurudolu/package.json
    package.json
  </files>
  <action>
    Verify and finalize MONO-04 per D-07 — each app declares its own Astro stack.

    Confirm `apps/vzhurudolu/package.json` contains ALL of:
    - `"astro": "4.16.19"` (exact)
    - `@astrojs/rss`, `@astrojs/sitemap`
    - `sharp`, `remark-gfm`, `rehype-raw` (if used in astro.config.mjs)
    - All other runtime/build deps from original root package.json

    Confirm root `package.json` has NO `dependencies` block with astro, sharp, or @astrojs/*.

    Structural verification commands (must all pass):
    - `node -e "const p=require('./apps/vzhurudolu/package.json'); if(p.dependencies?.astro!=='4.16.19') process.exit(1)"`
    - `node -e "const p=require('./package.json'); if(p.dependencies?.astro||p.devDependencies?.astro) process.exit(1)"`

    Simulate clean CI install: remove node_modules, run `npm ci`, then `npx turbo run build --filter=@vd/vzhurudolu`.

    Confirm Vite plugins resolve content: build log should NOT show "0 files validated" or empty prirucka output (cwd pitfall from RESEARCH Pitfall 1).
  </action>
  <acceptance_criteria>
    - astro 4.16.19 declared in app package.json only
    - Clean npm ci + turbo build succeeds without hoisting errors
    - dist/_astro/ populated after clean install build
    - Root package.json has no astro-related dependencies
  </acceptance_criteria>
  <verify>
    <automated>rm -rf node_modules apps/vzhurudolu/node_modules && npm ci && npx turbo run build --filter=@vd/vzhurudolu && test -d apps/vzhurudolu/dist/_astro</automated>
  </verify>
  <done>MONO-04 satisfied: app self-contained; clean install builds successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → root devDependencies | New packages turbo/typescript enter build toolchain |
| Turbo → app cwd | Task runner must execute astro in app directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering | turbo npm package | mitigate | Human legitimacy checkpoint (Task 1) before install; T-01-SC |
| T-01-03 | Tampering | typescript npm package | mitigate | Same checkpoint as turbo |
| T-01-04 | Denial of Service | Wrong build cwd | mitigate | Turbo filter + app-local scripts; verify dist/_astro/ after clean ci |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck unavailable; blocking human checkpoint for [ASSUMED] turbo/typescript |

</threat_model>

<verification>
- `npx turbo run build --filter=@vd/vzhurudolu` passes after clean `npm ci`
- Root has no astro deps; app has astro 4.16.19 exact
- turbo.json valid with tasks key
</verification>

<success_criteria>
- MONO-02: Filter commands build Czech app independently
- MONO-04: App declares own astro and @astrojs/* deps
</success_criteria>

## Artifacts This Phase Produces

| Artifact | Path |
|----------|------|
| Turborepo config | `turbo.json` |
| Root orchestrator | `package.json` (turbo scripts, engines.node >=22) |
| Root TypeScript base | `tsconfig.json` |
| Updated lockfile | `package-lock.json` |

<output>
Create `.planning/phases/01-monorepo-foundation-ci/01-02-SUMMARY.md` when done
</output>
