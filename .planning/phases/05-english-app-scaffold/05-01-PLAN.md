---
phase: 05-english-app-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/types/index.ts
  - packages/shared/layouts/BaseLayout.astro
  - packages/shared/components/Header.astro
  - packages/shared/components/Navigation.astro
  - packages/shared/components/Footer.astro
  - apps/vzhurudolu/package.json
autonomous: true
requirements:
  - EN-02
  - EN-07
user_setup: []

must_haves:
  truths:
    - "Shared layouts accept siteConfig so EN app can render Martin Michálek branding without forking components"
    - "Czech site still builds unchanged when pages omit siteConfig (CS defaults preserved)"
    - "EN siteConfig defines Articles and Martin nav items only"
  artifacts:
    - path: packages/shared/types/index.ts
      provides: "SiteConfig and NavItem types for per-app branding"
      exports: ["SiteConfig", "NavItem"]
    - path: packages/shared/layouts/BaseLayout.astro
      provides: "Optional siteConfig prop for lang, siteName, title suffix"
    - path: packages/shared/components/Header.astro
      provides: "Text wordmark mode when siteConfig.showLogoSymbol is false"
    - path: packages/shared/components/Navigation.astro
      provides: "Configurable navItems from siteConfig"
    - path: packages/shared/components/Footer.astro
      provides: "Minimal EN footer when siteConfig.footerMode is minimal"
  key_links:
    - from: packages/shared/layouts/BaseLayout.astro
      to: packages/shared/types/index.ts
      via: "SiteConfig type import"
      pattern: "SiteConfig"
    - from: apps/vzhurudolu/src/pages/index.astro
      to: packages/shared/layouts/BaseLayout.astro
      via: "existing import without siteConfig — CS defaults"
      pattern: "@vd/shared/layouts/BaseLayout"
---

## Phase Goal

**As a** visitor to michalek.dev, **I want to** see Martin Michálek · Web & Performance branding with Articles and Martin navigation, **so that** the English site feels like a personal brand separate from Vzhůru dolů.

<objective>
Parameterize shared layout/chrome components with optional siteConfig — foundation for EN branding (EN-02, EN-07) without breaking the Czech app.

Purpose: Phase 2 deferred siteConfig refactor (02-04-SUMMARY); EN app cannot ship with hardcoded CS strings in BaseLayout/Header/Footer.
Output: SiteConfig type, updated shared components with CS backward-compatible defaults, green @vd/vzhurudolu build.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-english-app-scaffold/05-CONTEXT.md
@.planning/phases/05-english-app-scaffold/05-UI-SPEC.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/02-shared-packages-extraction/02-04-SUMMARY.md
@packages/shared/types/index.ts
@packages/shared/layouts/BaseLayout.astro
@packages/shared/components/Header.astro
@packages/shared/components/Navigation.astro
@packages/shared/components/Footer.astro
@apps/vzhurudolu/src/pages/index.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define SiteConfig contract in @vd/shared/types</name>
  <files>packages/shared/types/index.ts</files>
  <read_first>
    - packages/shared/types/index.ts (existing CategoryConfig)
    - .planning/research/ARCHITECTURE.md (siteConfig example block)
    - .planning/phases/05-english-app-scaffold/05-UI-SPEC.md (Navigation & Chrome table)
    - .planning/phases/05-english-app-scaffold/05-CONTEXT.md (App Scaffold & Monorepo Integration)
  </read_first>
  <acceptance_criteria>
    - packages/shared/types/index.ts exports SiteConfig interface with fields: id, siteName, titleSuffix, locale, origin, navItems, showLogoSymbol, footerMode
    - NavItem type exported with label, href, activeMatch (string prefix or exact path pattern)
    - CategoryConfig export unchanged
    - packages/shared/package.json exports "./types" path already resolves (no export map change required unless missing)
  </acceptance_criteria>
  <action>
    Add SiteConfig and NavItem types to packages/shared/types/index.ts per ARCHITECTURE.md siteConfig pattern and 05-UI-SPEC nav table. SiteConfig.id is a string union 'vzhurudolu' | 'michalek-dev'. siteName for EN is "Martin Michálek". titleSuffix for EN is "Martin Michálek · Web & Performance". locale is BCP47 string ('cs' | 'en'). origin is absolute site URL. navItems is ordered NavItem array. showLogoSymbol boolean — false hides hammer SVG in Header. footerMode is 'full' | 'minimal' — full keeps current CS footer markup, minimal renders EN legal-only footer per UI-SPEC. Do not create apps/michalek-dev/site.config.ts in this task — types only.
  </action>
  <verify>
    <automated>grep -q 'export interface SiteConfig' packages/shared/types/index.ts && grep -q 'export interface NavItem' packages/shared/types/index.ts && grep -q 'export interface CategoryConfig' packages/shared/types/index.ts</automated>
  </verify>
  <done>SiteConfig and NavItem types exported from @vd/shared/types</done>
</task>

<task type="auto">
  <name>Task 2: Wire siteConfig into shared layout components with CS defaults</name>
  <files>packages/shared/layouts/BaseLayout.astro, packages/shared/components/Header.astro, packages/shared/components/Navigation.astro, packages/shared/components/Footer.astro</files>
  <read_first>
    - packages/shared/layouts/BaseLayout.astro (full file — hardcoded cs lang, Vzhůru dolů strings)
    - packages/shared/components/Header.astro (hammer SVG + Vzhůru dolů wordmark)
    - packages/shared/components/Navigation.astro (CS nav items hardcoded)
    - packages/shared/components/Footer.astro (CS footer sections)
    - .planning/phases/05-english-app-scaffold/05-UI-SPEC.md (Brand & Visual Identity, Navigation & Chrome, Footer)
  </read_first>
  <acceptance_criteria>
    - BaseLayout accepts optional siteConfig?: SiteConfig prop; html lang attribute uses siteConfig.locale defaulting to 'cs'
    - BaseLayout og:site_name, application-name, apple-mobile-web-app-title use siteConfig.siteName with CS fallback "Vzhůru dolů"
    - BaseLayout title rendering: when siteConfig.titleSuffix provided, append " · {titleSuffix}" to page title prop (EN pattern from UI-SPEC)
    - Header renders text-only wordmark "Martin Michálek" linking to / when showLogoSymbol is false; CS default keeps hammer SVG + Vzhůru dolů
    - Navigation renders navItems from siteConfig when provided; CS default keeps existing podcast/ebook/video nav unchanged
    - Footer footerMode minimal renders only Privacy / Cookies legal links and "Built by Martin Michálek" attribution per UI-SPEC copywriting contract
    - npm run build -w @vd/vzhurudolu exits 0 with no page import changes required (siteConfig optional everywhere)
  </acceptance_criteria>
  <action>
    Import SiteConfig from @vd/shared/types in each shared component. Add optional siteConfig prop with inline CS_DEFAULT constant mirroring current hardcoded behavior so existing vzhurudolu pages need zero edits. BaseLayout: set html lang from siteConfig.locale; parameterize meta site name fields; keep CS hreflang/cssmine link only when locale is cs (omit for en). Header: when showLogoSymbol false, skip SVG symbol block and render text wordmark from siteConfig.siteName with optional f-6 subtitle "Web & Performance" under name per UI-SPEC recommendation. Navigation: when siteConfig.navItems present, map items to nav markup with aria-current="page" when pathname matches activeMatch rules (Articles active on /, /blog/*, /guide/*; Martin active on /martin). Footer: minimal mode outputs Privacy → /privacy, Cookies → /cookies, attribution link to /martin — omit podcast/courses/socials/RSS/CSS Mine/VD symbol. Do not remove CS footer content from full mode. Do not add language switch UI.
  </action>
  <verify>
    <automated>npm run build -w @vd/vzhurudolu && test -d apps/vzhurudolu/dist/_astro</automated>
  </verify>
  <done>Shared components accept siteConfig; Czech build passes without page changes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared components → both apps | siteConfig props must not leak CS-only URLs into EN render path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | packages/shared/components/*.astro | mitigate | CS_DEFAULT fallback preserves vzhurudolu behavior; EN path only when siteConfig passed explicitly |
| T-05-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |

</threat_model>

<verification>
- @vd/vzhurudolu build green after shared component changes
- SiteConfig type exported and importable from @vd/shared/types
- No vzhurudolu page files modified (backward compatibility)
</verification>

<success_criteria>
- SiteConfig contract exists and shared chrome is parameterizable
- Czech site build unaffected
</success_criteria>

## Artifacts this phase produces

- `SiteConfig` interface — packages/shared/types/index.ts
- `NavItem` interface — packages/shared/types/index.ts
- Updated `BaseLayout.astro`, `Header.astro`, `Navigation.astro`, `Footer.astro` with optional siteConfig prop and CS defaults

<output>
Create `.planning/phases/05-english-app-scaffold/05-01-SUMMARY.md` when done
</output>
