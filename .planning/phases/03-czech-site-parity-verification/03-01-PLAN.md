---
phase: 03-czech-site-parity-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-czech-site-parity-verification/03-PUBLIC-ASSET-AUDIT.md
autonomous: true
requirements:
  - VD-05
user_setup: []

must_haves:
  truths:
    - "D-09: public/ inventory categorized (assets vs data/ vs files/ archives) with no deletions"
    - "D-10: Turbo build duration measured and documented against 5-minute threshold"
    - "D-11: vitePluginCopyPublicToDist() behavior preserved — audit documents copy gap, no optimization"
    - "D-12: 03-PUBLIC-ASSET-AUDIT.md exists with size table and Phase 4+ recommendations"
    - "VD-05: 685 MB public/ strategy documented without build timeout risk unaddressed"
  artifacts:
    - path: .planning/phases/03-czech-site-parity-verification/03-PUBLIC-ASSET-AUDIT.md
      provides: "Public asset inventory, build timing, FTP deploy exclusion notes"
      contains: "685 MB"
  key_links:
    - from: 03-PUBLIC-ASSET-AUDIT.md
      to: apps/vzhurudolu/public/
      via: du/find inventory commands
      pattern: "public/data/"
    - from: 03-PUBLIC-ASSET-AUDIT.md
      to: .github/workflows/deploy-ftp.yml
      via: FTP exclusion documentation
      pattern: "data/"
---

## Phase Goal

**As a** developer maintaining vzhurudolu.cz, **I want to** verify the monorepo Czech build matches live production, **so that** Vercel migration can proceed with confidence.

<objective>
Measure build performance and document the 685 MB `public/` asset strategy — first vertical slice proving VD-05 is understood before parity gates run.

Purpose: Resolves RESEARCH open question #3 (CI vs local build time); delivers D-09/D-10/D-11/D-12 audit artifact per locked decisions.
Output: `03-PUBLIC-ASSET-AUDIT.md` with verified size table, build timing, and Phase 4+ recommendations.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-czech-site-parity-verification/03-CONTEXT.md
@.planning/phases/03-czech-site-parity-verification/03-RESEARCH.md
@.planning/codebase/CONCERNS.md
@apps/vzhurudolu/astro.config.mjs
@apps/vzhurudolu/public/
@.github/workflows/deploy-ftp.yml
@.cursor/rules/archiv-public-data-files.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Timed build baseline (D-10)</name>
  <read_first>
    - .planning/phases/03-czech-site-parity-verification/03-RESEARCH.md (Pattern 4, D-10)
    - apps/vzhurudolu/astro.config.mjs (vitePluginCopyPublicToDist)
    - .github/workflows/pr-build.yml (CI build command)
  </read_first>
  <files>.planning/phases/03-czech-site-parity-verification/03-PUBLIC-ASSET-AUDIT.md</files>
  <action>
    Run timed monorepo build per D-10:

    `/usr/bin/time -p npx turbo run build --filter=@vd/vzhurudolu 2>&1 | tee /tmp/phase3-build-timing.log`

    Record real elapsed seconds from `time` output. Assert dist exists: `test -d apps/vzhurudolu/dist/_astro`.

    Create `03-PUBLIC-ASSET-AUDIT.md` skeleton with sections: Build Timing, Public Inventory, Dist Output, FTP Deploy Exclusions, Recommendations.

    Populate Build Timing section: total wall time, pass/fail vs 300s (5 min) threshold, note whether bottleneck is copy-public plugin or Astro SSG. Do NOT modify vitePluginCopyPublicToDist() per D-11.
  </action>
  <acceptance_criteria>
    - Build completes successfully
    - Elapsed time recorded in audit doc
    - Pass/fail vs 5-minute threshold explicitly stated
    - dist/_astro/ present after build
  </acceptance_criteria>
  <verify>
    <automated>test -d apps/vzhurudolu/dist/_astro && test -f .planning/phases/03-czech-site-parity-verification/03-PUBLIC-ASSET-AUDIT.md && grep -q "Build Timing" .planning/phases/03-czech-site-parity-verification/03-PUBLIC-ASSET-AUDIT.md</automated>
  </verify>
  <done>Build timing baseline captured in audit doc</done>
</task>

<task type="auto">
  <name>Task 2: Public/ inventory and dist size table (D-09, D-12)</name>
  <read_first>
    - .planning/phases/03-czech-site-parity-verification/03-RESEARCH.md (Verified inventory table)
    - .cursor/rules/archiv-public-data-files.md (read-only archives)
    - apps/vzhurudolu/public/ (do not modify any files)
  </read_first>
  <files>.planning/phases/03-czech-site-parity-verification/03-PUBLIC-ASSET-AUDIT.md</files>
  <action>
    Run read-only inventory per RESEARCH Pattern 4:

    - `du -sh apps/vzhurudolu/public apps/vzhurudolu/public/* | sort -hr`
    - `find apps/vzhurudolu/public -type f | wc -l`
    - Post-build: `du -sh apps/vzhurudolu/dist`
    - Confirm `test -f apps/vzhurudolu/dist/.htaccess`

    Add Public Inventory table to audit doc with columns: Path | Size | Category (Active/Archive) | Notes.

    Required rows: total public/, public/assets/, public/data/ (128 MB archive — do not modify), public/files/ (archive), public/prirucka/, public/.htaccess.

    Document FTP deploy exclusion from deploy-ftp.yml: data/** and files/** excluded from upload but still copied to dist via plugin — flag as Phase 4+ optimization gap per D-11.

    Do NOT delete or modify anything under public/data/ or public/files/ per archiv rule and D-09.
  </action>
  <acceptance_criteria>
    - Size table matches live disk measurements (within rounding)
    - Archives categorized as read-only
    - dist/.htaccess presence confirmed
    - FTP exclusion vs dist copy gap documented
  </acceptance_criteria>
  <verify>
    <automated>test -f apps/vzhurudolu/dist/.htaccess && grep -q "public/data/" .planning/phases/03-czech-site-parity-verification/03-PUBLIC-ASSET-AUDIT.md && grep -q "Archive" .planning/phases/03-czech-site-parity-verification/03-PUBLIC-ASSET-AUDIT.md</automated>
  </verify>
  <done>685 MB inventory documented with categorization</done>
</task>

<task type="auto">
  <name>Task 3: Phase 4+ recommendations and VD-05 closure</name>
  <read_first>
    - .planning/phases/03-czech-site-parity-verification/03-CONTEXT.md (D-11, D-13 deferred scope)
    - .planning/REQUIREMENTS.md (VD-05 definition)
  </read_first>
  <files>.planning/phases/03-czech-site-parity-verification/03-PUBLIC-ASSET-AUDIT.md</files>
  <action>
    Complete audit doc Recommendations section with actionable Phase 4+ items (no implementation in Phase 3):

    - Selective public/ copy excluding data/ and files/ for Vercel deploy
    - vercel.json redirect port (D-13 out of scope here — reference only)
    - Build cache strategy if CI exceeds 5 minutes

    Add summary verdict: VD-05 satisfied if build under 5 minutes OR bottleneck documented with mitigation plan. State explicitly that copy behavior unchanged per D-11.

    Resolve RESEARCH open question #3 in doc: record local timing; note CI timing will be captured when pr-build.yml runs full gate in Plan 04.
  </action>
  <acceptance_criteria>
    - Recommendations section present with at least 2 Phase 4+ items
    - VD-05 verdict stated
    - D-11 preservation explicitly confirmed
    - No files under public/ modified
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Recommendations" .planning/phases/03-czech-site-parity-verification/03-PUBLIC-ASSET-AUDIT.md && grep -q "VD-05" .planning/phases/03-czech-site-parity-verification/03-PUBLIC-ASSET-AUDIT.md</automated>
  </verify>
  <done>03-PUBLIC-ASSET-AUDIT.md complete — VD-05 audit slice deliverable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit scripts → public/ filesystem | Read-only du/find; no writes to archives |
| Build command → dist/ | Regenerates dist; no production access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Accidental modification of public/data/ archives | mitigate | Read-only commands only; archiv rule cited in plan |
| T-03-02 | Denial of Service | 685 MB copy causes CI timeout | mitigate | Measure and document per D-10; no optimization in Phase 3 |
| T-03-SC | Tampering | npm/turbo during build | accept | Existing toolchain from Phase 1; lockfile pinned |

</threat_model>

<verification>
- `03-PUBLIC-ASSET-AUDIT.md` exists with build timing, inventory table, recommendations
- `test -f apps/vzhurudolu/dist/.htaccess` after build
- No modifications under `apps/vzhurudolu/public/data/` or `public/files/`
</verification>

<success_criteria>
- VD-05 audit complete: 685 MB strategy documented, build timing measured
- Phase 4+ optimization paths identified without changing copy behavior
</success_criteria>

## Artifacts

| Artifact | Path | Provides |
|----------|------|----------|
| Public asset audit | `.planning/phases/03-czech-site-parity-verification/03-PUBLIC-ASSET-AUDIT.md` | Size table, build timing, FTP gap, recommendations |

<output>
Create `.planning/phases/03-czech-site-parity-verification/03-01-SUMMARY.md` when done
</output>
