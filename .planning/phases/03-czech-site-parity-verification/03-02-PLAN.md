---
phase: 03-czech-site-parity-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase3-parity.sh
autonomous: true
requirements:
  - VD-01
user_setup: []

must_haves:
  truths:
    - "D-01: URL inventory compared against live vzhurudolu.cz sitemap (not HTML diff)"
    - "D-02: Production sitemap fetched at verify time is baseline"
    - "D-03: Zero production sitemap paths missing from local dist/sitemap"
    - "D-18: verify-phase2-extraction.sh (and thus phase1) runs as first gate step"
    - "RESEARCH resolved: local-only sitemap extras emit WARN, not FAIL"
    - "Each local sitemap path maps to dist/{path}/index.html (trailingSlash: never)"
  artifacts:
    - path: scripts/verify-phase3-parity.sh
      provides: "Phase 3 parity gate composing phase2 + sitemap diff"
      min_lines: 40
    - path: apps/vzhurudolu/dist/sitemap.xml
      provides: "Local URL inventory for diff"
  key_links:
    - from: scripts/verify-phase3-parity.sh
      to: scripts/verify-phase2-extraction.sh
      via: bash delegation at script start
      pattern: "verify-phase2-extraction"
    - from: scripts/verify-phase3-parity.sh
      to: https://www.vzhurudolu.cz/sitemap.xml
      via: curl fetch for production baseline
      pattern: "vzhurudolu.cz/sitemap"
---

## Phase Goal

**As a** developer maintaining vzhurudolu.cz, **I want to** verify the monorepo Czech build matches live production, **so that** Vercel migration can proceed with confidence.

<objective>
Create the Phase 3 parity gate skeleton and implement sitemap URL inventory diff — first automated VD-01 slice comparing monorepo output to live production.

Purpose: Delivers D-01/D-02/D-03 URL parity gate; resolves sitemap extras policy (WARN not FAIL).
Output: `scripts/verify-phase3-parity.sh` with phase2 composition + sitemap diff + dist file existence checks.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-czech-site-parity-verification/03-CONTEXT.md
@.planning/phases/03-czech-site-parity-verification/03-RESEARCH.md
@scripts/verify-phase1-build.sh
@scripts/verify-phase2-extraction.sh
@apps/vzhurudolu/dist/sitemap.xml
@packages/shared/seo/custom-sitemap.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: verify-phase3-parity.sh skeleton + phase2 composition (D-04, D-18)</name>
  <read_first>
    - scripts/verify-phase2-extraction.sh (composition pattern)
    - scripts/verify-phase1-build.sh (dist preconditions)
    - .planning/phases/03-czech-site-parity-verification/03-RESEARCH.md (verify-phase3 skeleton)
  </read_first>
  <files>scripts/verify-phase3-parity.sh</files>
  <action>
    Create executable `scripts/verify-phase3-parity.sh` with `set -euo pipefail`, ROOT cd pattern matching phase2 script.

    Step 1: Require dist exists — if missing, print "run build first" and exit 1.

    Step 2: Delegate to `bash scripts/verify-phase2-extraction.sh` (covers phase1 draft/sitemap/_astro checks per D-18).

    Step 3: Assert `test -f apps/vzhurudolu/dist/.htaccess`.

    Add placeholder sections (commented stubs) for linkinator and redirect checks — activated in Plan 03.

    Script must fail if run before sitemap diff implemented (RED until Task 2 complete): exit 1 with message if sitemap diff function not yet wired.

    chmod +x the script.
  </action>
  <acceptance_criteria>
    - Script executable, follows phase2 ROOT/cd pattern
    - Calls verify-phase2-extraction.sh first
    - Fails clearly when dist/ missing
    - .htaccess dist check present
  </acceptance_criteria>
  <verify>
    <automated>test -x scripts/verify-phase3-parity.sh && grep -q "verify-phase2-extraction" scripts/verify-phase3-parity.sh</automated>
  </verify>
  <done>Phase 3 gate skeleton exists composing phase1+2 checks</done>
</task>

<task type="auto">
  <name>Task 2: Production vs local sitemap path diff (D-01, D-02)</name>
  <read_first>
    - .planning/phases/03-czech-site-parity-verification/03-RESEARCH.md (Pattern 1, Pitfall 1)
    - apps/vzhurudolu/astro.config.mjs (trailingSlash: never)
    - packages/shared/seo/custom-sitemap.ts (sitemap loc format)
  </read_first>
  <files>scripts/verify-phase3-parity.sh</files>
  <action>
    Implement sitemap diff per RESEARCH Pattern 1 using portable sed (not grep -P):

    extract_paths() function: sed -n 's|.*<loc>https://www.vzhurudolu.cz\(.*\)</loc>.*|\1|p' | sort -u

    Fetch production: `curl -sf "https://www.vzhurudolu.cz/sitemap.xml" -o "$PROD_SITEMAP"` — fail closed with clear message if curl fails (network required per D-02).

    Compare sets with comm:
    - MISSING = comm -23 prod local → **FAIL** (production paths absent from build) per D-03
    - EXTRAS = comm -13 local prod → **WARN only** (resolve RESEARCH open question #1: new local content OK, print first 20 paths, do NOT exit 1)

    Print counts: prod N, local M, missing count, extras count.

    Wire into verify-phase3-parity.sh after phase2 gate.
  </action>
  <acceptance_criteria>
    - Production sitemap fetched at verify time
    - Missing prod paths cause exit 1
    - Local-only extras print WARN and continue
    - Portable sed extraction (no grep -P)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/verify-phase3-parity.sh 2>&1 | head -5; test ${PIPESTATUS[0]} -eq 0 || test -d apps/vzhurudolu/dist</automated>
  </verify>
  <done>Sitemap URL inventory diff gate operational</done>
</task>

<task type="auto">
  <name>Task 3: Dist file existence map for local sitemap paths</name>
  <read_first>
    - .planning/phases/03-czech-site-parity-verification/03-RESEARCH.md (path_to_file helper)
    - apps/vzhurudolu/astro.config.mjs (trailingSlash: never)
  </read_first>
  <files>scripts/verify-phase3-parity.sh</files>
  <action>
    Add dist file existence verification for each path in local sitemap:

    path_to_file(): `/` → `apps/vzhurudolu/dist/index.html`; else → `apps/vzhurudolu/dist${path}/index.html`

    Loop local paths; if expected HTML missing, collect and FAIL with first 20 offenders.

    Run full gate assuming dist exists from prior build. Gate passes when: phase2 green, zero missing prod sitemap paths, all local sitemap paths have dist files, extras only WARN.

    Do NOT add linkinator or redirect checks yet — Plan 03 extends same file.
  </action>
  <acceptance_criteria>
    - Every local sitemap path resolves to existing dist HTML
    - Full script passes on current green build
    - Missing dist files cause exit 1 with path list
  </acceptance_criteria>
  <verify>
    <automated>npx turbo run build --filter=@vd/vzhurudolu && bash scripts/verify-phase3-parity.sh</automated>
  </verify>
  <done>VD-01 URL inventory slice complete — sitemap diff + dist existence green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verify script → production HTTPS | Fetches public sitemap only; no secrets |
| curl response → bash comm diff | Untrusted external XML parsed with sed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Spoofing | Production sitemap MITM | accept | HTTPS to known host; parity gate assumes DNS correct |
| T-03-04 | Denial of Service | curl failure blocks gate | mitigate | Fail closed with actionable message per RESEARCH Environment table |
| T-03-05 | Tampering | Malicious sitemap injection via MITM | accept | Low risk for public sitemap; HTTPS sufficient for Phase 3 |

</threat_model>

<verification>
- `npx turbo run build --filter=@vd/vzhurudolu && bash scripts/verify-phase3-parity.sh` passes
- Missing production paths FAIL; local extras WARN only
- verify-phase2-extraction.sh invoked at script start
</verification>

<success_criteria>
- Automated URL inventory parity gate operational
- Production baseline authoritative per D-02
- Sitemap extras policy resolved: WARN not FAIL
</success_criteria>

## Artifacts

| Artifact | Path | Provides |
|----------|------|----------|
| Parity gate (partial) | `scripts/verify-phase3-parity.sh` | Phase2 composition + sitemap diff + dist existence |
| Production baseline | Live `https://www.vzhurudolu.cz/sitemap.xml` | ~873 URL reference set |

<output>
Create `.planning/phases/03-czech-site-parity-verification/03-02-SUMMARY.md` when done
</output>
