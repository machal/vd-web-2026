---
phase: 03-czech-site-parity-verification
plan: 03
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - scripts/verify-phase3-parity.sh
  - scripts/redirect-samples.txt
autonomous: false
requirements:
  - VD-01
user_setup: []

must_haves:
  truths:
    - "D-03: Zero broken internal links in dist/ (linkinator gate)"
    - "D-05: Internal links only within apps/vzhurudolu/dist/"
    - "D-06: linkinator via npx on dist after build"
    - "D-07: Skips #anchors, mailto:, data:, /style/ paths"
    - "Top .htaccess redirects behave identically on live production (25-sample matrix)"
    - "RESEARCH resolved: skip external https except www.vzhurudolu.cz and root-relative /"
  artifacts:
    - path: scripts/verify-phase3-parity.sh
      provides: "Complete parity gate with linkinator + redirect checks"
    - path: scripts/redirect-samples.txt
      provides: "25 expected production redirect chains"
      min_lines: 20
  key_links:
    - from: scripts/verify-phase3-parity.sh
      to: apps/vzhurudolu/dist/
      via: npx linkinator@7.6.1 crawl
      pattern: "linkinator.*dist"
    - from: scripts/verify-phase3-parity.sh
      to: scripts/redirect-samples.txt
      via: curl -sI loop against production
      pattern: "redirect-samples"
    - from: scripts/redirect-samples.txt
      to: apps/vzhurudolu/public/.htaccess
      via: sampled Redirect/R=301 rules
      pattern: "prirucka/css3-flexbox"
---

## Phase Goal

**As a** developer maintaining vzhurudolu.cz, **I want to** verify the monorepo Czech build matches live production, **so that** Vercel migration can proceed with confidence.

<objective>
Extend verify-phase3-parity.sh with internal link crawl and production redirect sampling — completes automated VD-01 link and redirect parity slices.

Purpose: Delivers D-05/D-06/D-07 link gate and D-03 redirect parity via live production curl per D-02.
Output: Full automated gate minus CI wiring and manual spot-check (Plan 04).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-czech-site-parity-verification/03-CONTEXT.md
@.planning/phases/03-czech-site-parity-verification/03-RESEARCH.md
@scripts/verify-phase3-parity.sh
@apps/vzhurudolu/public/.htaccess
@apps/vzhurudolu/astro.config.mjs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve linkinator@7.6.1 package legitimacy [ASSUMED]</name>
  <read_first>
    - .planning/phases/03-czech-site-parity-verification/03-RESEARCH.md (Package Legitimacy Audit)
    - https://www.npmjs.com/package/linkinator
    - https://github.com/JustinBeckwith/linkinator
  </read_first>
  <files>scripts/verify-phase3-parity.sh</files>
  <action>
    Pause before first npx linkinator@7.6.1 use per RESEARCH Package Legitimacy Audit [ASSUMED] disposition.

    Human verifies: npmjs.com/package/linkinator publisher JustinBeckwith, version 7.6.1, no postinstall script; github.com/JustinBeckwith/linkinator active MIT repo.

    Resume signal "approved" unlocks Task 2 linkinator integration.
  </action>
  <acceptance_criteria>
    - Human confirmed linkinator@7.6.1 legitimacy on npm and GitHub
    - [ASSUMED] disposition accepted with slopcheck unavailable
  </acceptance_criteria>
  <what-built>Package legitimacy review for npx linkinator@7.6.1 before first CI/verify use</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/linkinator — confirm publisher JustinBeckwith, version 7.6.1, no postinstall script
    2. Open https://github.com/JustinBeckwith/linkinator — confirm active repo, MIT license
    3. Confirm slopcheck unavailable; [ASSUMED] disposition accepted per RESEARCH Package Legitimacy Audit
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint:human-verify — resume with approved"</automated>
  </verify>
  <done>Human approved linkinator@7.6.1 for pinned npx use in verify script and CI</done>
  <resume-signal>Type "approved" to proceed with pinned npx linkinator@7.6.1</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Internal link crawl via linkinator (D-05, D-06, D-07)</name>
  <read_first>
    - .planning/phases/03-czech-site-parity-verification/03-RESEARCH.md (Pattern 3, Pitfalls 2-3)
    - scripts/verify-phase3-parity.sh (insert after sitemap diff section)
    - apps/vzhurudolu/astro.config.mjs (trailingSlash: never)
  </read_first>
  <files>scripts/verify-phase3-parity.sh</files>
  <action>
    Add linkinator step to verify-phase3-parity.sh after sitemap checks per D-06 (linkinator chosen over lychee per RESEARCH — lowest CI footprint).

    Pin version: `npx --yes linkinator@7.6.1 apps/vzhurudolu/dist`

    Flags: `--recurse --clean-urls --verbosity error`

    Skip patterns per D-07 and RESEARCH open question #2:
    - `--skip '^mailto:'`
    - `--skip '^#'`
    - `--skip '^data:'`
    - `--skip '/style/'`
    - `--skip '^https://(?!www\.vzhurudolu\.cz)'` OR equivalent to skip all external https except same-host (internal-only per D-05)

    Disable fragment checking if linkinator flags anchors by default — anchors must not fail per D-07.

    On any broken link output, script exits 1. Print "Phase 3 parity gate: internal links OK" on success.

    Optional: add linkinator.config.json at repo root only if skip list exceeds inline flags — prefer inline in script for Phase 3.
  </action>
  <acceptance_criteria>
    - linkinator runs against dist/ with --clean-urls
    - D-07 skip patterns applied
    - External URLs skipped (internal-only scope)
    - Broken internal link causes gate failure
  </acceptance_criteria>
  <verify>
    <automated>npx turbo run build --filter=@vd/vzhurudolu && bash scripts/verify-phase3-parity.sh 2>&1 | grep -q "internal links OK"</automated>
  </verify>
  <done>Zero broken internal links gate operational per D-03</done>
</task>

<task type="auto">
  <name>Task 3: Production redirect sample matrix (D-01, D-03)</name>
  <read_first>
    - .planning/phases/03-czech-site-parity-verification/03-RESEARCH.md (Pattern 2, recommended 25 samples)
    - apps/vzhurudolu/public/.htaccess (Redirect and R=301 rules only)
  </read_first>
  <files>scripts/verify-phase3-parity.sh, scripts/redirect-samples.txt</files>
  <action>
    Create `scripts/redirect-samples.txt` with 25 `{source_path}|{expected_location_suffix}` pairs per RESEARCH sample categories:

    Shortcuts /p/, /b/, /k/, /v/, /f/ (5); e-book redirects (5); příručka typo fixes (5); legacy /data/ (3); kurzy consolidation (3); AMP strip (1); blog→podcast (1); misc /checklist, /lektori/martin-michalek, /frontend-frameworky (2).

    Verify each pair against live production with curl before committing expected values:
    `curl -sI "https://www.vzhurudolu.cz${source}" | grep -i '^location:'`

    Add redirect check loop to verify-phase3-parity.sh:
    - Read redirect-samples.txt (skip blank/# lines)
    - curl -sI each source against production (NOT dist — Apache not executed on static files per RESEARCH Pitfall 6)
    - Normalize Location header: strip host, compare suffix to expected
    - Accept 301/308; FAIL on mismatch or missing Location

    Exclude RewriteCond www/HTTPS/trailing-slash rules and RedirectMatch 404 per RESEARCH.

    Do NOT port redirects to vercel.json — D-13 out of scope.
  </action>
  <acceptance_criteria>
    - redirect-samples.txt has 25 verified production pairs
    - Script tests live production only (not dist)
    - Mismatched Location causes exit 1
    - Only explicit 301 Redirect/R=301 rules sampled
  </acceptance_criteria>
  <verify>
    <automated>test -f scripts/redirect-samples.txt && grep -v '^#' scripts/redirect-samples.txt | grep -v '^$' | wc -l | grep -qE '^2[0-9]|^25' && bash scripts/verify-phase3-parity.sh</automated>
  </verify>
  <done>Redirect parity sampling complete — full verify-phase3-parity.sh automated gate green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npx → npm registry | Supply chain for linkinator@7.6.1 |
| verify script → production HTTPS | Redirect HEAD requests to public URLs |
| redirect-samples.txt → git | Repo-controlled expected values only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | npx supply-chain on CI | mitigate | Pin linkinator@7.6.1; blocking human checkpoint before first use [ASSUMED] |
| T-03-07 | Tampering | Malicious redirect-samples.txt edit | mitigate | Git-tracked file; pairs verified against live production at creation |
| T-03-08 | Denial of Service | linkinator false positives on external links | mitigate | Skip external https per D-05; --clean-urls per Pitfall 3 |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for linkinator [ASSUMED] |

</threat_model>

<verification>
- Full `bash scripts/verify-phase3-parity.sh` passes after build
- linkinator finds zero broken internal links
- All 25 redirect samples match production Location headers
</verification>

<success_criteria>
- D-03 automated criteria met: zero broken internal links, redirect samples pass
- verify-phase3-parity.sh is complete automated gate (CI + spot-check in Plan 04)
</success_criteria>

## Artifacts

| Artifact | Path | Provides |
|----------|------|----------|
| Complete parity gate | `scripts/verify-phase3-parity.sh` | phase2 + sitemap + links + redirects |
| Redirect sample matrix | `scripts/redirect-samples.txt` | 25 production redirect expectations |

<output>
Create `.planning/phases/03-czech-site-parity-verification/03-03-SUMMARY.md` when done
</output>
