---
phase: 2
slug: shared-packages-extraction
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-07
---

# Phase 2 — UI Design Contract

> Move-only package extraction. No new visual or interaction design. Generated by gsd-ui-researcher in `--auto` mode.

---

## Phase Constraint: Move-Only

**Hard rule (D-10, D-11):** Phase 2 is file moves + import path updates only. Zero behavior changes, zero visual changes, zero refactors "while we're here."

| Constraint | Requirement |
|------------|-------------|
| Visual parity | Preserve existing rendered output byte-for-byte where possible |
| Behavioral parity | No new props, no changed defaults, no CSS/HTML edits |
| Token/color/layout | No new design tokens, colors, spacing, or layout changes |
| Verification | Full visual/URL parity diff deferred to **Phase 3 (VD-01)** |
| Build gate | Phase 2 must not break `turbo build --filter=@vd/vzhurudolu` |

**Source:** `02-CONTEXT.md` D-10, D-11; user directive (move-only, zero visual/behavioral changes).

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Astro 4 SSG — not React/shadcn) |
| Preset | not applicable |
| Component library | existing Astro `.astro` components |
| Icon library | inline SVG in existing components (unchanged) |
| Font | existing site CSS (stays in app — shared static assets deferred) |
| Styling | existing app CSS/assets — **not extracted in Phase 2** |

No `components.json` detected. shadcn gate: **skipped** — stack is Astro, not React/Next/Vite SPA.

---

## Components Moved Unchanged

These components move from `apps/vzhurudolu` to `packages/shared/components/` (or equivalent subpath per `exports` map). Markup, classes, styles, and copy remain identical.

| Component | Current Path | Notes |
|-----------|--------------|-------|
| `BaseLayout` | `src/layouts/BaseLayout.astro` | Page shell, meta/OG tags |
| `Header` | `src/components/Header.astro` | Site chrome, logo SVG |
| `Navigation` | `src/components/Navigation.astro` | **Transitive** — imported by `Header`; must move together |
| `Footer` | `src/components/Footer.astro` | Site footer |
| `ArticleHeader` | `src/components/ArticleHeader.astro` | Article page header |
| `ArticleFooter` | `src/components/ArticleFooter.astro` | Article page footer; type boundary fix only (no visual change) |

**Allowed non-visual change:** `ArticleFooter` may receive a minimal shared `CategoryConfig` interface instead of importing from `apps/vzhurudolu/src/data/categories.ts` (D-05 dependency rule). Prop shape and rendered output must remain identical.

**Out of scope (stay in app):** All page routes, content collections, `categories.ts` data, app-specific CSS/static assets.

**Source:** `02-CONTEXT.md` D-03, D-04; `02-RESEARCH.md` Header→Navigation dependency.

---

## Spacing Scale

**Not applicable — preserve existing.**

No new spacing tokens. Executor must not introduce or modify spacing values. Existing spacing comes from app CSS classes already referenced by moved components.

Exceptions: none (move-only).

---

## Typography

**Not applicable — preserve existing.**

No new font sizes, weights, or line heights. Existing typography defined in app stylesheets consumed by unchanged component markup.

---

## Color

**Not applicable — preserve existing.**

No new color tokens or palette changes. 60/30/10 contract does not apply — this phase does not design; it relocates.

Accent reserved for: **unchanged** — whatever existing components already use.

---

## Copywriting Contract

**Not applicable — preserve existing.**

| Element | Copy |
|---------|------|
| Primary CTA | unchanged — existing Czech labels in moved components |
| Empty state | unchanged — not introduced in this phase |
| Error state | unchanged — not introduced in this phase |
| Destructive confirmation | none in this phase |

No new user-facing strings. No copy edits.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not required |
| third-party | none | not required |

---

## Implementation Guardrails for Executor

1. **Move verbatim** — copy files; update import paths in app pages and `astro.config.mjs`.
2. **No CSS extraction** — shared static assets (`packages/shared/static`) deferred per `02-CONTEXT.md` deferred ideas.
3. **Import switch only** — e.g. `@vd/shared/components/BaseLayout.astro` replaces `../../layouts/BaseLayout.astro`.
4. **siteConfig injection** — if layouts need app config, inject via props with same rendered output (Claude's discretion per CONTEXT).
5. **Do not run visual diff** — Phase 3 (VD-01) owns parity verification.

---

## Checker Sign-Off

Move-only phases use a reduced checklist:

- [ ] Dimension 1 Copywriting: PASS (N/A — no new copy)
- [ ] Dimension 2 Visuals: PASS (preserve existing — no changes)
- [ ] Dimension 3 Color: PASS (N/A — no changes)
- [ ] Dimension 4 Typography: PASS (N/A — no changes)
- [ ] Dimension 5 Spacing: PASS (N/A — no changes)
- [ ] Dimension 6 Registry Safety: PASS (N/A — no shadcn)

**Approval:** pending — auto-approved for move-only constraint upon checker validation

---

*Phase: 02-Shared Packages Extraction*
*UI contract: move-only, zero design work*
*Parity verification: Phase 3 (VD-01)*
