---
phase: 02-shared-packages-extraction
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - packages/shared/package.json
  - packages/shared/markdown/create-markdown-config.ts
  - packages/shared/markdown/remark-heading-ids.ts
  - packages/shared/markdown/remark-process-markdown-attributes.ts
  - packages/shared/markdown/remark-prirucka-images.ts
  - packages/shared/markdown/remark-normalize-code-lang.ts
  - packages/shared/markdown/rehype-prirucka-images.ts
  - packages/shared/markdown/rehype-prirucka-links.ts
  - packages/shared/markdown/rehype-connected-elements.ts
  - packages/shared/markdown/rehype-related-to-inner-box.ts
  - packages/shared/markdown/rehype-remove-ebook-only.ts
  - packages/shared/markdown/rehype-remove-first-h1.ts
  - packages/shared/markdown/rehype-heading-anchors.ts
  - apps/vzhurudolu/astro.config.mjs
  - apps/vzhurudolu/src/utils/remark-*.ts
  - apps/vzhurudolu/src/utils/rehype-*.ts
autonomous: true
requirements:
  - MONO-03
user_setup: []

must_haves:
  truths:
    - "astro.config.mjs uses createMarkdownConfig({ contentPathPrefix: '/prirucka' }) per D-06"
    - "Markdown plugin order matches original astro.config.mjs lines 128-150 exactly"
    - "rehype-prirucka-links parameterized via collections option with CS defaults per D-07"
    - "No remark/rehype utility files remain in apps/vzhurudolu/src/utils/ for extracted modules"
    - "Czech prirucka pages build with identical markdown transforms"
  artifacts:
    - path: packages/shared/markdown/create-markdown-config.ts
      provides: "Factory returning markdown config for defineConfig"
      exports: ["createMarkdownConfig"]
    - path: packages/shared/markdown/rehype-prirucka-links.ts
      provides: "Parameterized internal link resolution"
      contains: "createRehypeContentLinks"
    - path: packages/shared/markdown/remark-prirucka-images.ts
      provides: "Parameterized guide image paths"
  key_links:
    - from: apps/vzhurudolu/astro.config.mjs
      to: packages/shared/markdown/create-markdown-config.ts
      via: createMarkdownConfig import
      pattern: "createMarkdownConfig\\(\\{.*contentPathPrefix: '/prirucka'"
    - from: packages/shared/markdown/create-markdown-config.ts
      to: packages/shared/markdown/rehype-prirucka-links.ts
      via: plugin array in exact order
      pattern: "rehypePriruckaLinks"
---

## Phase Goal

**As a** developer maintaining both Czech and English Astro apps, **I want to** import shared markdown pipeline, Vite plugins, layouts, and SEO helpers from `@vd/shared`, **so that** plugin code is not duplicated and the EN app can reuse the same infrastructure in Phase 5.

<objective>
Extract the remark/rehype markdown pipeline into `@vd/shared` with `createMarkdownConfig()` factory — highest-risk slice; plugin order is the contract.

Purpose: Delivers D-03 (markdown utils), D-06 (factory pattern), D-07 (path parameterization), D-10 (move-only with factory wrappers only where required).
Output: 11 moved utils + factory, astro.config wired, CS defaults preserve behavior.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-packages-extraction/02-CONTEXT.md
@.planning/phases/02-shared-packages-extraction/02-RESEARCH.md
@.planning/phases/02-shared-packages-extraction/02-PATTERNS.md
@.planning/research/PITFALLS.md
@apps/vzhurudolu/astro.config.mjs
@apps/vzhurudolu/src/utils/remark-process-markdown-attributes.ts
@apps/vzhurudolu/src/utils/rehype-prirucka-links.ts
@apps/vzhurudolu/src/utils/remark-prirucka-images.ts
@.cursor/rules/content-odkazovani.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move markdown utilities verbatim to @vd/shared</name>
  <read_first>
    - apps/vzhurudolu/astro.config.mjs (markdown block lines 113-150 — plugin order contract)
    - apps/vzhurudolu/src/utils/ (all remark-*.ts and rehype-*.ts files)
    - .planning/phases/02-shared-packages-extraction/02-RESEARCH.md (12-file inventory)
  </read_first>
  <files>
    packages/shared/markdown/remark-heading-ids.ts
    packages/shared/markdown/remark-process-markdown-attributes.ts
    packages/shared/markdown/remark-prirucka-images.ts
    packages/shared/markdown/remark-normalize-code-lang.ts
    packages/shared/markdown/rehype-prirucka-images.ts
    packages/shared/markdown/rehype-prirucka-links.ts
    packages/shared/markdown/rehype-connected-elements.ts
    packages/shared/markdown/rehype-related-to-inner-box.ts
    packages/shared/markdown/rehype-remove-ebook-only.ts
    packages/shared/markdown/rehype-remove-first-h1.ts
    packages/shared/markdown/rehype-heading-anchors.ts
    packages/shared/package.json
  </files>
  <action>
    Move-only copy of 11 markdown utility files from `apps/vzhurudolu/src/utils/` to `packages/shared/markdown/`. Include `remark-heading-ids.ts` even though disabled in config — required by remark-process-markdown-attributes.

    Update intra-package imports to relative paths within `packages/shared/markdown/` (e.g. `./remark-heading-ids.ts`).

    Add dependencies to `packages/shared/package.json`: remark-gfm ^4.0.1, rehype-raw ^7.0.0, unist-util-visit ^5.1.0, hast-util-to-text ^4.0.2, hast-util-from-html ^2.0.3, remark-parse ^11.0.0, remark-rehype ^11.1.2, rehype-stringify ^10.0.1, unified ^11.0.0, @types/mdast ^4.0.4.

    Do NOT change plugin logic yet — parameterization in Task 2. Do NOT delete app originals until Task 3.
  </action>
  <acceptance_criteria>
    - 11 files exist under packages/shared/markdown/
    - remark-process imports remark-heading-ids via relative path
    - All markdown deps declared in @vd/shared package.json
    - No imports from apps/* (D-05)
  </acceptance_criteria>
  <verify>
    <automated>ls packages/shared/markdown/*.ts | wc -l | awk '{exit ($1 < 11)}'</automated>
  </verify>
  <done>Markdown utilities copied to @vd/shared with dependencies declared</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create createMarkdownConfig factory with path parameterization</name>
  <read_first>
    - apps/vzhurudolu/astro.config.mjs (shikiConfig + plugin arrays)
    - packages/shared/markdown/rehype-prirucka-links.ts (CONTENT_ENTITIES hardcoding)
    - packages/shared/markdown/remark-prirucka-images.ts and rehype-prirucka-images.ts (/prirucka/images paths)
    - .planning/phases/02-shared-packages-extraction/02-CONTEXT.md (D-06, D-07, D-10)
  </read_first>
  <files>
    packages/shared/markdown/create-markdown-config.ts
    packages/shared/markdown/remark-prirucka-images.ts
    packages/shared/markdown/rehype-prirucka-images.ts
    packages/shared/markdown/rehype-prirucka-links.ts
  </files>
  <behavior>
    - createMarkdownConfig() with no args returns plugin arrays identical to current astro.config.mjs
    - createMarkdownConfig({ contentPathPrefix: '/prirucka' }) produces same /prirucka link targets as before
    - remarkRehype.allowDangerousHtml remains true
    - shikiConfig theme and langAlias unchanged
  </behavior>
  <action>
    Create `packages/shared/markdown/create-markdown-config.ts` per D-06.

    Export `MarkdownConfigOptions` with: contentPathPrefix (default '/prirucka'), guideImagesPrefix (default '/prirucka/images'), collections (default ['prirucka','blog','podcast']), includeEbookOnly (default true).

    Export `createMarkdownConfig(opts)` returning `{ shikiConfig, remarkPlugins, remarkRehype, rehypePlugins }` with EXACT plugin order from astro.config.mjs lines 128-150.

    Parameterize internals only where D-07 requires — wrap bodies, keep export names stable per D-10:
    - remark-prirucka-images / rehype-prirucka-images: replace literal `/prirucka/images` with `imagesPrefix` defaulting to `/prirucka/images`
    - rehype-prirucka-links: add `createRehypeContentLinks({ collections })` factory; default collections produce identical CONTENT_ENTITIES and path detection

    Import remarkGfm and rehypeRaw from package dependencies inside factory file.

    Defaults with no options must produce byte-identical CS output — no behavior change for Czech site.
  </action>
  <acceptance_criteria>
    - Factory exports createMarkdownConfig and MarkdownConfigOptions
    - Plugin order matches original config exactly
    - allowDangerousHtml: true preserved
    - Parameterized plugins use defaults matching current /prirucka behavior
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'createMarkdownConfig' packages/shared/markdown/create-markdown-config.ts && grep -q "contentPathPrefix" packages/shared/markdown/create-markdown-config.ts && grep -q 'allowDangerousHtml: true' packages/shared/markdown/create-markdown-config.ts</automated>
  </verify>
  <done>createMarkdownConfig factory ready with CS defaults and EN-ready parameters</done>
</task>

<task type="auto">
  <name>Task 3: Wire astro.config and remove app markdown originals</name>
  <read_first>
    - apps/vzhurudolu/astro.config.mjs (full import block lines 5-19)
    - apps/vzhurudolu/package.json (devDependencies to dedupe later)
  </read_first>
  <files>
    apps/vzhurudolu/astro.config.mjs
    apps/vzhurudolu/src/utils/remark-heading-ids.ts
    apps/vzhurudolu/src/utils/remark-process-markdown-attributes.ts
    apps/vzhurudolu/src/utils/remark-prirucka-images.ts
    apps/vzhurudolu/src/utils/remark-normalize-code-lang.ts
    apps/vzhurudolu/src/utils/rehype-prirucka-images.ts
    apps/vzhurudolu/src/utils/rehype-prirucka-links.ts
    apps/vzhurudolu/src/utils/rehype-connected-elements.ts
    apps/vzhurudolu/src/utils/rehype-related-to-inner-box.ts
    apps/vzhurudolu/src/utils/rehype-remove-ebook-only.ts
    apps/vzhurudolu/src/utils/rehype-remove-first-h1.ts
    apps/vzhurudolu/src/utils/rehype-heading-anchors.ts
  </files>
  <action>
    Replace markdown imports and inline config block in `apps/vzhurudolu/astro.config.mjs`:

    - Remove individual remark/rehype imports from `./src/utils/`
    - Add: `import { createMarkdownConfig } from '@vd/shared/markdown'`
    - Replace `markdown: { ... }` block with: `markdown: createMarkdownConfig({ contentPathPrefix: '/prirucka', collections: ['prirucka', 'blog', 'podcast'] })`

    Delete all 11 moved files from `apps/vzhurudolu/src/utils/`. Keep app-local: is-published.ts, get-related-articles.ts, validate-prirucka.ts, extract-content-fallback.ts, changed-files-integration.ts, custom-sitemap.ts (SEO moves in Plan 04).

    Run full build gate — prirucka content is highest regression risk (Pitfall 9).
  </action>
  <acceptance_criteria>
    - astro.config.mjs calls createMarkdownConfig with contentPathPrefix '/prirucka'
    - No extracted remark/rehype files in apps/vzhurudolu/src/utils/
    - is-published.ts and get-related-articles.ts remain app-local
    - Full Czech build passes
  </acceptance_criteria>
  <verify>
    <automated>grep -q "createMarkdownConfig({ contentPathPrefix: '/prirucka'" apps/vzhurudolu/astro.config.mjs && npx turbo run build --filter=@vd/vzhurudolu && bash scripts/verify-phase2-extraction.sh</automated>
  </verify>
  <done>Markdown pipeline extraction complete — factory wired, CS behavior preserved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown content → HTML output | allowDangerousHtml: true — trusted author content in git |
| Internal .md links → URLs | rehype-prirucka-links transforms user content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | allowDangerousHtml pipeline | accept | Existing trusted-content model; preserve unchanged per D-10 |
| T-02-05 | Tampering | Plugin order change | mitigate | Factory copies exact array from astro.config.mjs; build gate catches regressions |
| T-02-SC | Tampering | remark/rehype npm packages | mitigate | [ASSUMED] per RESEARCH audit; pinned versions in @vd/shared |

</threat_model>

<verification>
- createMarkdownConfig visible in astro.config.mjs with /prirucka prefix
- Full turbo build + verify-phase2-extraction.sh pass
- No extracted markdown files in app utils
</verification>

<success_criteria>
- Markdown pipeline lives in packages/shared/markdown/
- Factory parameterizes paths with CS defaults
- Czech SSG build succeeds with prirucka content
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Plan |
|----------|------|------|
| Markdown factory | `packages/shared/markdown/create-markdown-config.ts` | 03 |
| Remark/rehype plugins (11) | `packages/shared/markdown/*.ts` | 03 |
| Wired app config | `apps/vzhurudolu/astro.config.mjs` | 03 |

<output>
Create `.planning/phases/02-shared-packages-extraction/02-03-SUMMARY.md` when done
</output>
