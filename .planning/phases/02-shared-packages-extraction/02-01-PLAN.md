---
phase: 02-shared-packages-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/package.json
  - packages/shared/types/index.ts
  - scripts/verify-phase2-extraction.sh
  - apps/vzhurudolu/package.json
  - apps/vzhurudolu/astro.config.mjs
autonomous: true
requirements:
  - MONO-03
user_setup: []

must_haves:
  truths:
    - "Developer can npm install at repo root and workspace links @vd/shared to apps/vzhurudolu"
    - "Czech site still builds after @vd/shared scaffold with no file moves yet"
    - "astro.config.mjs declares vite.ssr.noExternal for @vd/shared per D-09"
    - "Phase 2 extraction verify script exists and fails until extraction completes"
  artifacts:
    - path: packages/shared/package.json
      provides: "@vd/shared workspace package with source exports map"
      contains: '"name": "@vd/shared"'
    - path: packages/shared/types/index.ts
      provides: "CategoryConfig interface for shared components"
      exports: ["CategoryConfig"]
    - path: scripts/verify-phase2-extraction.sh
      provides: "Phase 2 stale-path and import gate"
      min_lines: 15
    - path: apps/vzhurudolu/package.json
      provides: "Workspace dependency on @vd/shared"
      contains: '"@vd/shared": "*"'
  key_links:
    - from: apps/vzhurudolu/package.json
      to: packages/shared/package.json
      via: npm workspaces @vd/shared dependency
      pattern: '"@vd/shared": "\\*"'
    - from: apps/vzhurudolu/astro.config.mjs
      to: "@vd/shared"
      via: vite.ssr.noExternal
      pattern: "noExternal.*@vd/shared"
---

## Phase Goal

**As a** developer maintaining both Czech and English Astro apps, **I want to** import shared markdown pipeline, Vite plugins, layouts, and SEO helpers from `@vd/shared`, **so that** plugin code is not duplicated and the EN app can reuse the same infrastructure in Phase 5.

<objective>
Scaffold `@vd/shared` workspace package with source exports, wire the Czech app, and establish the Phase 2 verification gate — the foundation slice before any file moves.

Purpose: Delivers D-01 (single package), D-08 (source exports), D-09 (noExternal), D-12 (@vd/* namespace) without breaking the existing build.
Output: Linked `@vd/shared` package, verify script scaffold, passing `npm run build:vzhurudolu`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-packages-extraction/02-CONTEXT.md
@.planning/phases/02-shared-packages-extraction/02-RESEARCH.md
@.planning/phases/02-shared-packages-extraction/02-PATTERNS.md
@.planning/phases/02-shared-packages-extraction/02-VALIDATION.md
@.planning/phases/01-monorepo-foundation-ci/01-04-SUMMARY.md
@package.json
@apps/vzhurudolu/package.json
@apps/vzhurudolu/astro.config.mjs
@apps/vzhurudolu/src/data/categories.ts
@scripts/verify-phase1-build.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 2 extraction verify gate (Wave 0 scaffold)</name>
  <read_first>
    - scripts/verify-phase1-build.sh (gate structure to extend)
    - .planning/phases/02-shared-packages-extraction/02-VALIDATION.md (Wave 0 requirements)
    - .planning/phases/02-shared-packages-extraction/02-RESEARCH.md (files to extract inventory)
  </read_first>
  <files>scripts/verify-phase2-extraction.sh</files>
  <action>
    Create executable `scripts/verify-phase2-extraction.sh` as the Phase 2 end-to-end gate per VALIDATION.md Wave 0.

    Script must:
    1. Source or call Phase 1 checks — run `bash scripts/verify-phase1-build.sh` first (dist/_astro, draft exclusion, sitemap).
    2. Assert `packages/shared/package.json` exists with name `@vd/shared`.
    3. Assert `apps/vzhurudolu/package.json` contains `"@vd/shared": "*"`.
    4. Assert `apps/vzhurudolu/astro.config.mjs` contains `noExternal` referencing `@vd/shared`.
    5. Placeholder section (commented or failing checks) for stale-path assertions to activate in Plan 05: old vite-plugin paths at app root, old utils paths for moved markdown/seo files.

    Run script before scaffold completes — it MUST fail on missing `@vd/shared` package (RED gate).
  </action>
  <acceptance_criteria>
    - Script is executable with `set -euo pipefail`
    - Script fails when packages/shared/package.json absent
    - Script delegates dist smoke checks to verify-phase1-build.sh
    - Stale-path check placeholders documented for Plan 05 activation
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/verify-phase2-extraction.sh; test $? -ne 0</automated>
  </verify>
  <done>verify-phase2-extraction.sh exists and fails until @vd/shared scaffold lands</done>
</task>

<task type="auto">
  <name>Task 2: @vd/shared package scaffold and workspace wiring</name>
  <read_first>
    - .planning/phases/02-shared-packages-extraction/02-CONTEXT.md (D-01, D-08, D-12)
    - .planning/phases/02-shared-packages-extraction/02-RESEARCH.md (package.json skeleton)
    - apps/vzhurudolu/src/data/categories.ts (CategoryConfig shape for types/index.ts)
    - packages/.gitkeep (replace with real package)
  </read_first>
  <files>
    packages/shared/package.json
    packages/shared/types/index.ts
    packages/shared/markdown/.gitkeep
    packages/shared/vite-plugins/.gitkeep
    packages/shared/seo/.gitkeep
    packages/shared/layouts/.gitkeep
    packages/shared/components/.gitkeep
    apps/vzhurudolu/package.json
    apps/vzhurudolu/astro.config.mjs
  </files>
  <action>
    Create `@vd/shared` per D-01 and D-08 (source exports, no tsup/tsc build step).

    1. Remove `packages/.gitkeep`; create `packages/shared/package.json` with name `@vd/shared`, version `0.0.0`, `private: true`, `type: module`, and exports map per RESEARCH skeleton: `./markdown`, `./markdown/*`, `./vite-plugins/*`, `./seo/custom-sitemap`, `./layouts/*`, `./components/*`, `./types`. Declare peerDependencies `astro: 4.16.19` and `vite: ^5.4.0`. Do NOT add dependencies yet — Plan 02/03 move files with their deps.

    2. Create `packages/shared/types/index.ts` exporting `CategoryConfig` interface matching `apps/vzhurudolu/src/data/categories.ts` lines 1-8 (slug, title, description, hasTOC, tags). Data array stays app-local per D-04/D-05.

    3. Create empty subfolder placeholders (`.gitkeep` ok) for markdown, vite-plugins, seo, layouts, components.

    4. Add `"@vd/shared": "*"` to `apps/vzhurudolu/package.json` dependencies.

    5. Add `vite.ssr.noExternal: ['@vd/shared']` to `apps/vzhurudolu/astro.config.mjs` per D-09. Do NOT change existing plugin imports yet.

    6. Run `npm install` at repo root to link workspace.
  </action>
  <acceptance_criteria>
    - `@vd/shared` resolves via npm workspaces
    - exports map covers all planned subpaths
    - CategoryConfig interface matches app categories.ts shape
    - noExternal present in astro.config.mjs
    - No file moves in this task — build behavior unchanged
  </acceptance_criteria>
  <verify>
    <automated>npm install && npm run build:vzhurudolu</automated>
  </verify>
  <done>@vd/shared linked; Czech build passes with scaffold only</done>
</task>

<task type="auto">
  <name>Task 3: Activate partial Phase 2 verify gate</name>
  <read_first>
    - scripts/verify-phase2-extraction.sh (from Task 1)
    - scripts/verify-phase1-build.sh
  </read_first>
  <files>scripts/verify-phase2-extraction.sh</files>
  <action>
    Update `scripts/verify-phase2-extraction.sh` so scaffold checks (package exists, workspace dep, noExternal) pass after Task 2. Stale-path checks for moved files remain skipped or commented — they activate in Plan 05.

    Run full gate: `npx turbo run build --filter=@vd/vzhurudolu && bash scripts/verify-phase2-extraction.sh`.
  </action>
  <acceptance_criteria>
    - verify-phase2-extraction.sh exits 0 after scaffold
    - verify-phase1-build.sh checks still pass inside gate
    - Stale-path assertions not yet enforced (documented for Plan 05)
  </acceptance_criteria>
  <verify>
    <automated>npx turbo run build --filter=@vd/vzhurudolu && bash scripts/verify-phase2-extraction.sh</automated>
  </verify>
  <done>Phase 2 foundation slice verified — workspace wired, build green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → monorepo | Workspace packages resolve via `*` protocol, not external registry |
| astro.config.mjs → Vite SSR | Shared package must be bundled via noExternal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | @vd/shared package name | mitigate | Private monorepo; workspace `*` only; no publish |
| T-02-02 | Tampering | package.json exports map | mitigate | Source-controlled; verify script checks package name |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new external packages in Plan 01; slopcheck when available |

</threat_model>

<verification>
- `npm run build:vzhurudolu` passes
- `bash scripts/verify-phase2-extraction.sh` passes scaffold checks
- No behavior change to markdown, plugins, or layouts (imports unchanged)
</verification>

<success_criteria>
- @vd/shared workspace package exists with exports map
- Czech app declares workspace dependency and noExternal
- Build and dist smoke checks pass
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Plan |
|----------|------|------|
| Shared package manifest | `packages/shared/package.json` | 01 |
| Shared types stub | `packages/shared/types/index.ts` | 01 |
| Extraction verify script | `scripts/verify-phase2-extraction.sh` | 01 |
| App workspace wiring | `apps/vzhurudolu/package.json`, `astro.config.mjs` | 01 |

<output>
Create `.planning/phases/02-shared-packages-extraction/02-01-SUMMARY.md` when done
</output>
