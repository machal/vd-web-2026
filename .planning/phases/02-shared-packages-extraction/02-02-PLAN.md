---
phase: 02-shared-packages-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/shared/package.json
  - packages/shared/vite-plugins/vite-plugin-validate-frontmatter.ts
  - packages/shared/vite-plugins/vite-plugin-prirucka-images.ts
  - packages/shared/vite-plugins/vite-plugin-content-images.ts
  - packages/shared/vite-plugins/vite-plugin-design-images.ts
  - apps/vzhurudolu/astro.config.mjs
  - apps/vzhurudolu/vite-plugin-validate-frontmatter.ts
  - apps/vzhurudolu/vite-plugin-prirucka-images.ts
  - apps/vzhurudolu/vite-plugin-content-images.ts
  - apps/vzhurudolu/vite-plugin-design-images.ts
autonomous: true
requirements:
  - MONO-03
user_setup: []

must_haves:
  truths:
    - "Four Vite plugins import from @vd/shared/vite-plugins/* in astro.config.mjs"
    - "No vite-plugin-*.ts files remain at apps/vzhurudolu/ root"
    - "Czech build still converts images and validates frontmatter via shared plugins"
    - "process.cwd() paths still resolve to app root during turbo build"
  artifacts:
    - path: packages/shared/vite-plugins/vite-plugin-validate-frontmatter.ts
      provides: "Frontmatter validation at buildStart"
      exports: ["vitePluginValidateFrontmatter"]
    - path: packages/shared/vite-plugins/vite-plugin-prirucka-images.ts
      provides: "Prirucka image WebP conversion"
      exports: ["vitePluginPriruckaImages"]
    - path: packages/shared/vite-plugins/vite-plugin-content-images.ts
      provides: "Content image WebP conversion"
      exports: ["vitePluginContentImages"]
    - path: packages/shared/vite-plugins/vite-plugin-design-images.ts
      provides: "Design SVG/raster optimization"
      exports: ["vitePluginDesignImages"]
  key_links:
    - from: apps/vzhurudolu/astro.config.mjs
      to: packages/shared/vite-plugins/
      via: "@vd/shared/vite-plugins/*" imports
      pattern: "@vd/shared/vite-plugins/vite-plugin"
    - from: packages/shared/vite-plugins/vite-plugin-validate-frontmatter.ts
      to: apps/vzhurudolu/src/content
      via: path.resolve('src/content') with app cwd
      pattern: "path\\.resolve\\('src/content'\\)"
---

## Phase Goal

**As a** developer maintaining both Czech and English Astro apps, **I want to** import shared markdown pipeline, Vite plugins, layouts, and SEO helpers from `@vd/shared`, **so that** plugin code is not duplicated and the EN app can reuse the same infrastructure in Phase 5.

<objective>
Extract four Vite plugins into `@vd/shared` — first shared-code vertical slice where build plugins run from the workspace package.

Purpose: Delivers D-03 (Vite plugins), D-05 (shared never imports apps), D-10 (move-only) for the plugin layer.
Output: Plugins in `packages/shared/vite-plugins/`, app imports updated, originals deleted, build green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-packages-extraction/02-CONTEXT.md
@.planning/phases/02-shared-packages-extraction/02-RESEARCH.md
@.planning/phases/02-shared-packages-extraction/02-PATTERNS.md
@apps/vzhurudolu/astro.config.mjs
@apps/vzhurudolu/vite-plugin-validate-frontmatter.ts
@apps/vzhurudolu/vite-plugin-prirucka-images.ts
@apps/vzhurudolu/vite-plugin-content-images.ts
@apps/vzhurudolu/vite-plugin-design-images.ts
@packages/shared/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move Vite plugins verbatim to @vd/shared</name>
  <read_first>
    - apps/vzhurudolu/vite-plugin-validate-frontmatter.ts
    - apps/vzhurudolu/vite-plugin-prirucka-images.ts
    - apps/vzhurudolu/vite-plugin-content-images.ts
    - apps/vzhurudolu/vite-plugin-design-images.ts
    - .planning/phases/02-shared-packages-extraction/02-RESEARCH.md (process.cwd() notes)
  </read_first>
  <files>
    packages/shared/vite-plugins/vite-plugin-validate-frontmatter.ts
    packages/shared/vite-plugins/vite-plugin-prirucka-images.ts
    packages/shared/vite-plugins/vite-plugin-content-images.ts
    packages/shared/vite-plugins/vite-plugin-design-images.ts
    packages/shared/package.json
  </files>
  <action>
    Move-only extraction per D-10 — copy file contents verbatim to `packages/shared/vite-plugins/`. Do NOT rewrite path resolution; plugins use `process.cwd()` which resolves to app root during `turbo build --filter=@vd/vzhurudolu`.

    Add devDependencies to `packages/shared/package.json`: `sharp: ^0.33.0`, `chokidar: ^3.6.0`, `svgo: ^3.3.2`. Keep `vite` as peerDependency only.

    Preserve export function names: `vitePluginValidateFrontmatter`, `vitePluginPriruckaImages`, `vitePluginContentImages`, `vitePluginDesignImages`.

    Do NOT move `vitePluginCopyPublicToDist` — stays inline in astro.config.mjs per RESEARCH.
  </action>
  <acceptance_criteria>
    - Four plugin files exist under packages/shared/vite-plugins/
    - Plugin bodies unchanged except import path adjustments within package
    - sharp/chokidar/svgo declared in @vd/shared package.json
    - No imports from apps/* in shared files (D-05)
  </acceptance_criteria>
  <verify>
    <automated>test -f packages/shared/vite-plugins/vite-plugin-validate-frontmatter.ts && test -f packages/shared/vite-plugins/vite-plugin-content-images.ts && grep -q 'vitePluginValidateFrontmatter' packages/shared/vite-plugins/vite-plugin-validate-frontmatter.ts</automated>
  </verify>
  <done>Four Vite plugins copied to @vd/shared with deps declared</done>
</task>

<task type="auto">
  <name>Task 2: Wire astro.config.mjs to @vd/shared Vite plugins</name>
  <read_first>
    - apps/vzhurudolu/astro.config.mjs (lines 20-23, 152-159)
    - packages/shared/package.json (exports map)
  </read_first>
  <files>apps/vzhurudolu/astro.config.mjs</files>
  <action>
    Replace local Vite plugin imports in `apps/vzhurudolu/astro.config.mjs`:

    - `import { vitePluginValidateFrontmatter } from '@vd/shared/vite-plugins/vite-plugin-validate-frontmatter'`
    - `import { vitePluginPriruckaImages } from '@vd/shared/vite-plugins/vite-plugin-prirucka-images'`
    - `import { vitePluginContentImages } from '@vd/shared/vite-plugins/vite-plugin-content-images'`
    - `import { vitePluginDesignImages } from '@vd/shared/vite-plugins/vite-plugin-design-images'`

    Keep vite.plugins array order identical. Keep `vitePluginCopyPublicToDist()` inline unchanged.

    Run `npm install` at root if package.json deps changed.
  </action>
  <acceptance_criteria>
    - astro.config.mjs has zero imports from `./vite-plugin-*.ts`
    - Plugin registration order unchanged
    - inline copy-public plugin still present
  </acceptance_criteria>
  <verify>
    <automated>npm run build:vzhurudolu</automated>
  </verify>
  <done>Czech build passes with Vite plugins from @vd/shared</done>
</task>

<task type="auto">
  <name>Task 3: Delete app-root plugin originals</name>
  <read_first>
    - apps/vzhurudolu/astro.config.mjs (confirm no local plugin imports)
    - scripts/verify-phase2-extraction.sh
  </read_first>
  <files>
    apps/vzhurudolu/vite-plugin-validate-frontmatter.ts
    apps/vzhurudolu/vite-plugin-prirucka-images.ts
    apps/vzhurudolu/vite-plugin-content-images.ts
    apps/vzhurudolu/vite-plugin-design-images.ts
  </files>
  <action>
    Delete the four original `apps/vzhurudolu/vite-plugin-*.ts` files after build passes with shared imports.

    Grep repo for stale imports: `grep -r "from './vite-plugin" apps/vzhurudolu` must return empty.

    Run full gate: `npx turbo run build --filter=@vd/vzhurudolu && bash scripts/verify-phase2-extraction.sh`.
  </action>
  <acceptance_criteria>
    - No vite-plugin-*.ts at apps/vzhurudolu root
    - No stale local import paths
    - Full build + verify gate passes
  </acceptance_criteria>
  <verify>
    <automated>! test -f apps/vzhurudolu/vite-plugin-content-images.ts && npx turbo run build --filter=@vd/vzhurudolu && bash scripts/verify-phase2-extraction.sh</automated>
  </verify>
  <done>Vite plugin extraction complete — shared package owns all four plugins</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vite plugin → filesystem | Plugins read/write app-local paths via process.cwd() |
| sharp native binary | Supply-chain dependency for image conversion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | Image plugin file writes | accept | Trusted build context; paths scoped to app tree |
| T-02-04 | Tampering | sharp/chokidar/svgo versions | mitigate | Pin versions in @vd/shared; npm ci in CI |
| T-02-SC | Tampering | npm installs (sharp, chokidar, svgo) | mitigate | Packages [ASSUMED] per RESEARCH audit; verify on npmjs.com before install |

</threat_model>

<verification>
- `npm run build:vzhurudolu` passes with plugins from @vd/shared
- `dist/_astro/` populated per verify-phase1-build.sh
- No duplicate vite-plugin files at app root
</verification>

<success_criteria>
- All four Vite plugins live in packages/shared/vite-plugins/
- astro.config.mjs imports exclusively from @vd/shared
- Czech SSG build succeeds
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Plan |
|----------|------|------|
| Vite plugins (4) | `packages/shared/vite-plugins/*.ts` | 02 |
| Plugin deps in shared | `packages/shared/package.json` devDependencies | 02 |
| Updated app config | `apps/vzhurudolu/astro.config.mjs` | 02 |

<output>
Create `.planning/phases/02-shared-packages-extraction/02-02-SUMMARY.md` when done
</output>
