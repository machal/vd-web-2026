---
phase: 02-shared-packages-extraction
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - packages/shared/seo/custom-sitemap.ts
  - packages/shared/layouts/BaseLayout.astro
  - packages/shared/components/Header.astro
  - packages/shared/components/Navigation.astro
  - packages/shared/components/Footer.astro
  - packages/shared/components/ArticleHeader.astro
  - packages/shared/components/ArticleFooter.astro
  - apps/vzhurudolu/astro.config.mjs
  - apps/vzhurudolu/src/utils/custom-sitemap.ts
  - apps/vzhurudolu/src/layouts/BaseLayout.astro
  - apps/vzhurudolu/src/components/Header.astro
  - apps/vzhurudolu/src/components/Navigation.astro
  - apps/vzhurudolu/src/components/Footer.astro
  - apps/vzhurudolu/src/components/ArticleHeader.astro
  - apps/vzhurudolu/src/components/ArticleFooter.astro
  - apps/vzhurudolu/src/pages/
autonomous: true
requirements:
  - MONO-03
user_setup: []

must_haves:
  truths:
    - "createCustomSitemap({ site }) replaces hardcoded Czech origin in sitemap integration"
    - "BaseLayout, Header, Footer, ArticleHeader, ArticleFooter, Navigation import from @vd/shared"
    - "All ~40 page routes compile with shared layout imports"
    - "ArticleFooter uses CategoryConfig from @vd/shared/types not app data path"
    - "is-published.ts and get-related-articles.ts remain app-local in page imports"
  artifacts:
    - path: packages/shared/seo/custom-sitemap.ts
      provides: "Parameterized sitemap AstroIntegration"
      exports: ["createCustomSitemap"]
    - path: packages/shared/layouts/BaseLayout.astro
      provides: "Shared page shell with slots"
    - path: packages/shared/components/Header.astro
      provides: "Site header with Navigation"
    - path: packages/shared/components/Navigation.astro
      provides: "Transitive Header dependency"
    - path: packages/shared/components/ArticleFooter.astro
      provides: "Article footer with categories prop"
  key_links:
    - from: apps/vzhurudolu/src/pages/blog/[slug].astro
      to: packages/shared/layouts/BaseLayout.astro
      via: "@vd/shared/layouts/BaseLayout.astro" import
      pattern: "@vd/shared/layouts/BaseLayout"
    - from: packages/shared/components/ArticleFooter.astro
      to: packages/shared/types/index.ts
      via: CategoryConfig type import
      pattern: "@vd/shared/types"
    - from: apps/vzhurudolu/astro.config.mjs
      to: packages/shared/seo/custom-sitemap.ts
      via: createCustomSitemap({ site })
      pattern: "createCustomSitemap"
---

## Phase Goal

**As a** developer maintaining both Czech and English Astro apps, **I want to** import shared markdown pipeline, Vite plugins, layouts, and SEO helpers from `@vd/shared`, **so that** plugin code is not duplicated and the EN app can reuse the same infrastructure in Phase 5.

<objective>
Extract SEO sitemap integration and shared layout/chrome components — complete the presentation and discovery slice so pages import all chrome from `@vd/shared`.

Purpose: Delivers D-03 (SEO helpers + layouts/components), D-05 (CategoryConfig type decoupling), D-10 (move-only, no siteConfig refactor).
Output: createCustomSitemap factory, 6 moved .astro files, ~40 page import updates, build green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-packages-extraction/02-CONTEXT.md
@.planning/phases/02-shared-packages-extraction/02-RESEARCH.md
@.planning/phases/02-shared-packages-extraction/02-PATTERNS.md
@apps/vzhurudolu/src/utils/custom-sitemap.ts
@apps/vzhurudolu/src/layouts/BaseLayout.astro
@apps/vzhurudolu/src/components/Header.astro
@apps/vzhurudolu/src/components/Navigation.astro
@apps/vzhurudolu/src/components/ArticleFooter.astro
@apps/vzhurudolu/src/pages/blog/[slug].astro
@apps/vzhurudolu/src/data/categories.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract custom sitemap with createCustomSitemap factory</name>
  <read_first>
    - apps/vzhurudolu/src/utils/custom-sitemap.ts (hardcoded site URL line 14)
    - apps/vzhurudolu/astro.config.mjs (integrations block)
    - .planning/phases/02-shared-packages-extraction/02-RESEARCH.md (createCustomSitemap sketch)
  </read_first>
  <files>
    packages/shared/seo/custom-sitemap.ts
    apps/vzhurudolu/astro.config.mjs
    apps/vzhurudolu/src/utils/custom-sitemap.ts
  </files>
  <action>
    Move `apps/vzhurudolu/src/utils/custom-sitemap.ts` to `packages/shared/seo/custom-sitemap.ts` per D-03.

    Wrap with factory per D-06: export `createCustomSitemap(options: { site: string })` returning AstroIntegration. Replace hardcoded `const site = 'https://www.vzhurudolu.cz'` with `options.site` throughout hook body. Preserve all page filtering logic (style/, 404 exclusion) and XML generation verbatim.

    Update `apps/vzhurudolu/astro.config.mjs`:
    - `import { createCustomSitemap } from '@vd/shared/seo/custom-sitemap'`
    - `integrations: [createCustomSitemap({ site: 'https://www.vzhurudolu.cz' }), changedFilesIntegration()]`

    Delete app original after build passes. Keep changed-files-integration.ts app-local.

    Do NOT re-enable @astrojs/sitemap.
  </action>
  <acceptance_criteria>
    - createCustomSitemap accepts site parameter
    - CS app passes same URL as before
    - sitemap.xml still generated in dist/
    - changed-files-integration stays app-local
  </acceptance_criteria>
  <verify>
    <automated>npm run build:vzhurudolu && test -f apps/vzhurudolu/dist/sitemap.xml</automated>
  </verify>
  <done>SEO sitemap integration lives in @vd/shared with parameterized site URL</done>
</task>

<task type="auto">
  <name>Task 2: Move layout and chrome components to @vd/shared</name>
  <read_first>
    - apps/vzhurudolu/src/components/Header.astro (Navigation import)
    - apps/vzhurudolu/src/components/ArticleFooter.astro (CategoryConfig import)
    - packages/shared/types/index.ts
    - .planning/phases/02-shared-packages-extraction/02-RESEARCH.md (6-component inventory)
  </read_first>
  <files>
    packages/shared/layouts/BaseLayout.astro
    packages/shared/components/Header.astro
    packages/shared/components/Navigation.astro
    packages/shared/components/Footer.astro
    packages/shared/components/ArticleHeader.astro
    packages/shared/components/ArticleFooter.astro
    apps/vzhurudolu/src/layouts/BaseLayout.astro
    apps/vzhurudolu/src/components/Header.astro
    apps/vzhurudolu/src/components/Navigation.astro
    apps/vzhurudolu/src/components/Footer.astro
    apps/vzhurudolu/src/components/ArticleHeader.astro
    apps/vzhurudolu/src/components/ArticleFooter.astro
  </files>
  <action>
    Move-only extraction per D-10 of 6 files:
    - BaseLayout.astro → packages/shared/layouts/
    - Header.astro, Navigation.astro, Footer.astro, ArticleHeader.astro, ArticleFooter.astro → packages/shared/components/

    Header and Navigation MUST move in same commit — Header imports `./Navigation.astro`; keep relative import after move.

    In ArticleFooter.astro: replace `import type { CategoryConfig } from '../data/categories'` with `import type { CategoryConfig } from '@vd/shared/types'`. Keep `categories` data array passed as prop from pages — app imports `categories` from `../../data/categories`.

    Do NOT inject siteConfig or refactor hardcoded CS branding — defer to Phase 5 per RESEARCH discretion.

    Copy files to packages/shared/ but KEEP app originals until Task 3 updates all page imports and build passes — then delete app originals in Task 3 (atomic with import rewrites).
  </action>
  <acceptance_criteria>
    - All 6 .astro files exist under packages/shared/
    - Navigation moves with Header
    - ArticleFooter imports CategoryConfig from @vd/shared/types only
    - No imports from apps/* in shared components (D-05)
    - CS hardcoded strings preserved unchanged
    - App originals still present (repo buildable until Task 3)
  </acceptance_criteria>
  <verify>
    <automated>test -f packages/shared/components/Navigation.astro && test -f packages/shared/layouts/BaseLayout.astro && grep -q '@vd/shared/types' packages/shared/components/ArticleFooter.astro && test -f apps/vzhurudolu/src/layouts/BaseLayout.astro</automated>
  </verify>
  <done>Layout and chrome components copied to @vd/shared (app originals retained for Task 3)</done>
</task>

<task type="auto">
  <name>Task 3: Update page imports and remove app originals</name>
  <read_first>
    - apps/vzhurudolu/src/pages/blog/[slug].astro (import pattern)
    - .planning/phases/02-shared-packages-extraction/02-PATTERNS.md (target imports)
    - Grep results for layouts/BaseLayout imports across src/pages/
  </read_first>
  <files>apps/vzhurudolu/src/pages/</files>
  <action>
    Update all page files under `apps/vzhurudolu/src/pages/` that import moved components:

    Replace:
    - `../../layouts/BaseLayout.astro` or `../layouts/BaseLayout.astro` → `@vd/shared/layouts/BaseLayout.astro`
    - `../../components/Header.astro` → `@vd/shared/components/Header.astro`
    - `../../components/Footer.astro` → `@vd/shared/components/Footer.astro`
    - `../../components/ArticleHeader.astro` → `@vd/shared/components/ArticleHeader.astro`
    - `../../components/ArticleFooter.astro` → `@vd/shared/components/ArticleFooter.astro`

    Keep app-local imports unchanged:
    - `categories` from `../../data/categories`
    - `getRelatedArticles` from `../../utils/get-related-articles`
    - `isPublished` from `../../utils/is-published`

    Grep gate: no page imports from `../../layouts/` or `../../components/Header` etc. for moved files.

    After build passes, delete app originals:
    - apps/vzhurudolu/src/layouts/BaseLayout.astro
    - apps/vzhurudolu/src/components/Header.astro, Navigation.astro, Footer.astro, ArticleHeader.astro, ArticleFooter.astro

    Run full build gate again after deletion.
  </action>
  <acceptance_criteria>
    - All pages using BaseLayout/Header/Footer import from @vd/shared
    - Article pages import ArticleHeader/ArticleFooter from @vd/shared
    - is-published and get-related-articles imports remain app-local
    - No stale layout/component paths in pages
    - Full build passes
  </acceptance_criteria>
  <verify>
    <automated>grep -r "layouts/BaseLayout" apps/vzhurudolu/src/pages/ | grep -v '@vd/shared' | wc -l | awk '{exit ($1 != 0)}' && npx turbo run build --filter=@vd/vzhurudolu && bash scripts/verify-phase2-extraction.sh</automated>
  </verify>
  <done>All page routes compile with shared layouts and SEO from @vd/shared</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sitemap hook → dist/sitemap.xml | Build-time file write |
| Layout components → static HTML | No user input at render time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | sitemap.xml URLs | accept | Public URLs only; same as production behavior |
| T-02-07 | Tampering | sitemap site parameter | mitigate | App passes explicit site URL from defineConfig.site |
| T-02-SC | Tampering | npm installs | mitigate | No new packages in Plan 04 |

</threat_model>

<verification>
- dist/sitemap.xml exists and excludes draft slug
- No local layout/component imports in pages for moved files
- Full turbo build + verify-phase2-extraction.sh pass
</verification>

<success_criteria>
- SEO and layout chrome import from @vd/shared
- ~40 pages compile successfully
- sitemap generation unchanged for CS site
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Plan |
|----------|------|------|
| Sitemap factory | `packages/shared/seo/custom-sitemap.ts` | 04 |
| Layout shell | `packages/shared/layouts/BaseLayout.astro` | 04 |
| Chrome components (5) | `packages/shared/components/*.astro` | 04 |
| Updated page imports | `apps/vzhurudolu/src/pages/**/*.astro` | 04 |

<output>
Create `.planning/phases/02-shared-packages-extraction/02-04-SUMMARY.md` when done
</output>
