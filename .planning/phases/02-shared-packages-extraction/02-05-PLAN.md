---
phase: 02-shared-packages-extraction
plan: 05
type: execute
wave: 5
depends_on:
  - 02-04
files_modified:
  - apps/vzhurudolu/package.json
  - scripts/verify-phase2-extraction.sh
  - apps/vzhurudolu/astro.config.mjs
  - apps/vzhurudolu/src/utils/
  - apps/vzhurudolu/src/layouts/
  - apps/vzhurudolu/src/components/
autonomous: true
requirements:
  - MONO-03
user_setup: []

must_haves:
  truths:
    - "D-13: Content stays app-local — src/content/ never moved to packages/shared"
    - "No duplicated plugin, markdown, SEO, or layout source files remain in app tree"
    - "verify-phase2-extraction.sh enforces stale-path checks and passes"
    - "Duplicate markdown deps removed from app package.json where @vd/shared owns them"
    - "MONO-03 satisfied: shared infrastructure in packages, Czech app imports @vd/shared exclusively for extracted code"
  artifacts:
    - path: scripts/verify-phase2-extraction.sh
      provides: "Complete Phase 2 extraction gate with stale-path assertions"
    - path: packages/shared/
      provides: "Complete @vd/shared package with all extracted modules"
  key_links:
    - from: scripts/verify-phase2-extraction.sh
      to: packages/shared/
      via: stale path negation checks
      pattern: "test ! -f apps/vzhurudolu/vite-plugin"
    - from: apps/vzhurudolu/astro.config.mjs
      to: packages/shared/
      via: all @vd/shared imports for extracted modules
      pattern: "@vd/shared"
---

## Phase Goal

**As a** developer maintaining both Czech and English Astro apps, **I want to** import shared markdown pipeline, Vite plugins, layouts, and SEO helpers from `@vd/shared`, **so that** plugin code is not duplicated and the EN app can reuse the same infrastructure in Phase 5.

<objective>
Final cleanup and verification — activate full Phase 2 gate, remove duplicate dependencies, confirm MONO-03 with zero stale extraction paths.

Purpose: Delivers D-10 completion, D-11 scope boundary (build gate only, parity deferred to Phase 3), closes Wave 0 verify script gaps.
Output: Clean app tree, passing verify-phase2-extraction.sh, PR-ready extraction.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-shared-packages-extraction/02-CONTEXT.md
@.planning/phases/02-shared-packages-extraction/02-RESEARCH.md
@.planning/phases/02-shared-packages-extraction/02-VALIDATION.md
@scripts/verify-phase2-extraction.sh
@apps/vzhurudolu/package.json
@.github/workflows/pr-build.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate full stale-path checks in verify script</name>
  <read_first>
    - scripts/verify-phase2-extraction.sh
    - .planning/phases/02-shared-packages-extraction/02-RESEARCH.md (files explicitly staying in app)
    - .planning/phases/02-shared-packages-extraction/02-VALIDATION.md
  </read_first>
  <files>scripts/verify-phase2-extraction.sh</files>
  <action>
    Complete `scripts/verify-phase2-extraction.sh` per VALIDATION.md Wave 0:

    After verify-phase1-build.sh, assert ABSENT (exit 1 if present):
    - apps/vzhurudolu/vite-plugin-validate-frontmatter.ts
    - apps/vzhurudolu/vite-plugin-prirucka-images.ts
    - apps/vzhurudolu/vite-plugin-content-images.ts
    - apps/vzhurudolu/vite-plugin-design-images.ts
    - apps/vzhurudolu/src/utils/custom-sitemap.ts
    - apps/vzhurudolu/src/utils/remark-process-markdown-attributes.ts
    - apps/vzhurudolu/src/utils/rehype-prirucka-links.ts
    - apps/vzhurudolu/src/layouts/BaseLayout.astro
    - apps/vzhurudolu/src/components/Header.astro
    - apps/vzhurudolu/src/components/Navigation.astro

    Assert PRESENT in astro.config.mjs (grep):
    - createMarkdownConfig
    - createCustomSitemap
    - @vd/shared/vite-plugins
    - noExternal.*@vd/shared

    Assert PRESENT app-local (must NOT be deleted):
    - apps/vzhurudolu/src/utils/is-published.ts
    - apps/vzhurudolu/src/utils/get-related-articles.ts
    - apps/vzhurudolu/src/utils/changed-files-integration.ts

    Use grep -v '^#' for comment-safe counts where applicable.
  </action>
  <acceptance_criteria>
    - All stale-path checks active and passing
    - App-local files explicitly preserved
    - @vd/shared import assertions in astro.config.mjs
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/verify-phase2-extraction.sh</automated>
  </verify>
  <done>Phase 2 verify gate fully enforced</done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate deps and empty app directories</name>
  <read_first>
    - apps/vzhurudolu/package.json (devDependencies)
    - packages/shared/package.json (owned deps)
    - apps/vzhurudolu/astro.config.mjs (confirm no direct remark/rehype imports)
  </read_first>
  <files>
    apps/vzhurudolu/package.json
    apps/vzhurudolu/src/layouts/
    apps/vzhurudolu/src/components/
  </files>
  <action>
    Dedupe dependencies per RESEARCH recommendation:

    Remove from `apps/vzhurudolu/package.json` devDependencies any packages now owned exclusively by @vd/shared AND no longer imported directly in app code: remark-gfm, rehype-raw, unist-util-visit, hast-util-to-text, hast-util-from-html, remark-parse, remark-rehype, rehype-stringify, unified, @types/mdast, sharp, chokidar, svgo — only if grep confirms zero direct imports in apps/vzhurudolu outside astro.config factory wiring.

    Remove empty `apps/vzhurudolu/src/layouts/` directory if BaseLayout was sole file. Remove moved component files from `src/components/` but KEEP app-local components (ValidationErrors.astro etc.).

    Run `npm install` at root after package.json change.
  </action>
  <acceptance_criteria>
    - No duplicate markdown/plugin deps in app if unused
    - App-local utils and components preserved
    - npm install succeeds
  </acceptance_criteria>
  <verify>
    <automated>npm install && npm run build:vzhurudolu</automated>
  </verify>
  <done>Dependency graph clean — @vd/shared owns shared deps</done>
</task>

<task type="auto">
  <name>Task 3: Final MONO-03 gate and repo-wide stale import scan</name>
  <read_first>
    - .github/workflows/pr-build.yml (CI command sequence)
    - .planning/REQUIREMENTS.md (MONO-03 definition)
  </read_first>
  <files>apps/vzhurudolu/</files>
  <action>
    Run final extraction gate matching PR CI:

    `npx turbo run build --filter=@vd/vzhurudolu && bash scripts/verify-phase2-extraction.sh`

    Repo-wide grep for stale imports (must return empty):
    - `from './src/utils/rehype-` in astro.config.mjs
    - `from './src/utils/custom-sitemap'`
    - `from './vite-plugin-` in astro.config.mjs
    - `from '../../layouts/BaseLayout` in pages (without @vd/shared)

    Confirm MONO-03: markdown pipeline, Vite plugins, layouts, SEO helpers live in packages/shared — Czech app imports @vd/shared for all four categories.

    Update 02-VALIDATION.md task statuses if executor convention requires (optional — do not block on doc update).

    Visual/URL parity (VD-01) explicitly out of scope per D-11 — note in SUMMARY for Phase 3 handoff.
  </action>
  <acceptance_criteria>
    - Full CI-equivalent gate passes
    - Zero stale import paths for extracted modules
    - MONO-03 traceability complete
    - is-published.ts and get-related-articles.ts confirmed app-local
  </acceptance_criteria>
  <verify>
    <automated>npx turbo run build --filter=@vd/vzhurudolu && bash scripts/verify-phase2-extraction.sh && ! grep -q "from './vite-plugin" apps/vzhurudolu/astro.config.mjs</automated>
  </verify>
  <done>Phase 2 extraction complete — MONO-03 satisfied, ready for Phase 3 parity verification</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI verify script → repo filesystem | Read-only assertions on paths |
| Dependency deduplication → node_modules | npm install resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | Accidental deletion of app-local utils | mitigate | verify script asserts is-published.ts and get-related-articles.ts present |
| T-02-09 | Denial of Service | Broken dep removal breaks build | mitigate | Build gate after package.json edit |
| T-02-SC | Tampering | npm installs during dedupe | mitigate | npm ci compatible; lockfile updated via npm install |

</threat_model>

<verification>
- `npx turbo run build --filter=@vd/vzhurudolu && bash scripts/verify-phase2-extraction.sh` passes
- No stale extraction paths in app tree
- PR CI command sequence succeeds locally
</verification>

<success_criteria>
- MONO-03 complete: shared code in packages/shared, no duplication in app
- verify-phase2-extraction.sh fully active
- Czech build green; Phase 3 parity verification can begin
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Plan |
|----------|------|------|
| Complete verify gate | `scripts/verify-phase2-extraction.sh` | 05 |
| Cleaned app deps | `apps/vzhurudolu/package.json` | 05 |
| Full @vd/shared package | `packages/shared/**` (all modules) | 01-05 cumulative |

### Phase 2 cumulative deliverables

| Category | Location | Count |
|----------|----------|-------|
| Vite plugins | `packages/shared/vite-plugins/` | 4 |
| Markdown utils + factory | `packages/shared/markdown/` | 12 |
| SEO integration | `packages/shared/seo/custom-sitemap.ts` | 1 |
| Layouts | `packages/shared/layouts/BaseLayout.astro` | 1 |
| Components | `packages/shared/components/` | 5 |
| Shared types | `packages/shared/types/index.ts` | 1 |
| Verify script | `scripts/verify-phase2-extraction.sh` | 1 |

<output>
Create `.planning/phases/02-shared-packages-extraction/02-05-SUMMARY.md` when done
</output>
